# Spec 05.0 — Workout Session Lifecycle (API)

**Status:** Draft v0.2 — one architect review applied (2026-09-20); no further
review gates outstanding.
**Last updated:** 2026-09-20
**Design refs:** DESIGN.md §4.0 (cross-cutting conventions — calendar fields,
numeric precision), §4.4 (`workout` / `workout_exercise` — the hot path),
§4.5 (PRs are written at finish — deferred to Spec 07 here, see §6), §4.9
(deletion matrix), §6 (idempotency via `client_generated_id`, `/v1` REST
conventions)

---

## 1. Purpose, scope & non-goals

Ship the **session lifecycle** of workout logging: the `workout` and
`workout_exercise` tables, the endpoints that start, resume, edit, finish and
delete a workout session and that add, reorder and remove exercises within it,
and the `@sin/core` types those endpoints need. This is the first of three
specs that split the roadmap's Spec 05 (05.0 session lifecycle, 05.1 set
logging, 05.2 rate limiting and write quotas), because the original scope
bundled several work sessions' worth of change. It also fixes the two rules
every later logging feature leans on: workout creation is idempotent under
retry (DESIGN §6), and `local_date` is derived once, at write time, from the
client's own offset (DESIGN §4.0).

### In scope

- The `workout` and `workout_exercise` tables, in migration
  `0005_create_workout_session`.
- The session lifecycle endpoints: create, read the active session, read by
  id, update (including finish), delete; add, update (reorder) and delete a
  workout exercise.
- Workout-creation idempotency via `client_generated_id`, and the
  one-in-progress-session-per-user rule.
- The calendar-field rules (`local_date` / `tz_offset_minutes`): how they are
  supplied, derived and stored.
- The `@sin/core` additions those need: `WorkoutId` / `WorkoutExerciseId`,
  `WORKOUT_SOURCE_VALUES`, the workout DTOs, and a new `time.ts` module of pure
  calendar helpers.

### Non-goals

- **`set_entry` and everything measure-shaped** — the table, per-modality
  validation, and the finish integrity rule ("a working set needs at least one
  measure") — are **Spec 05.1**. 05.0's finish transition only sets
  `ended_at`.
- **Rate limiting and per-user write quotas** are **Spec 05.2**, which also
  closes 03.2's open D20 (connection-pool blast radius) and D21 (unbounded
  storage growth via create/delete cycling). 05.0 ships no write throttle.
- **The paginated history list and all PR/progress work** are **Spec 07**.
  There is deliberately no `GET /v1/workouts` list here, and finishing a
  workout does not write PRs; 07 adds the PR recompute to the finish
  transition and backfills workouts finished before it ships.
- **`routine_id` and `superset_group`** semantics are **Spec 09**. Neither
  column is created here (see §4 and §12).
- **The logging UI** — active-session screen, exercise picker integration,
  resume flow — is **Spec 06**.

---

## 2. Acceptance criteria

Every behavioral criterion below gets ≥1 test naming its number
(`describe("AC7 — …")`) — mostly integration (Testcontainers) plus `@sin/core`
and route-level unit tests. AC20 is part test, part CI-pipeline check, and AC21
is verified by review of the doc diff. §10 maps each criterion to its method.

1. **Migration creates the session schema.**
   `0005_create_workout_session` applies cleanly on a fresh database and on top
   of the existing migration chain, and creates `workout` and
   `workout_exercise` with exactly the columns, types and nullability of §4;
   the three FKs (`workout.user_id → user(id)`,
   `workout_exercise.workout_id → workout(id)`,
   `workout_exercise.exercise_id → exercise(id)`) all `ON DELETE CASCADE`; the
   two `CHECK`s; and the five named indexes — `workout_user_client_id_key`
   (unique), `workout_user_started_idx`, `workout_user_active_key` (unique,
   partial on `ended_at IS NULL`), `workout_exercise_position_key` (a unique
   **constraint**, `DEFERRABLE INITIALLY IMMEDIATE`) and
   `workout_exercise_exercise_idx`. `prisma validate` passes with the `Workout`
   / `WorkoutExercise` models and the `User.workouts` /
   `Exercise.workoutExercises` back-references, and there is **no drift between
   the migrated database and `schema.prisma`**.
2. **Both `CHECK` literal lists are drift-guarded against `@sin/core`.**
   A raw insert of a value outside the list is rejected by the database on both
   `workout.source` and `workout_exercise.modality_snapshot`, and each member of
   `WORKOUT_SOURCE_VALUES` / `MODALITY_VALUES` is accepted. The drift guard is
   03.1 AC2's technique applied to **both** lists: an assertion that the
   migration `.sql` file **contains** the clause rendered from the frozen
   constant — string containment, not a parse of `pg_get_constraintdef()`, which
   Postgres normalizes to `= ANY (ARRAY[...])`.
3. **Workout creation is idempotent under retry.** A first
   `POST /v1/workouts` returns `201` with `Location: /v1/workouts/{id}`. A
   replay with the same `(user, clientGeneratedId)` returns `200` with the
   **stored** row and **no `Location` header**, even when the replayed body
   differs from the stored row (the body is ignored, never compared, never
   `409`). Exactly one row exists afterwards, and this holds when the replays
   are concurrent rather than sequential: a concurrent same-key burst returns
   one `201` and the rest `200`, never a `409`, because the `409` path
   re-reads by the idempotency key before it answers (§6.2, D50).
4. **At most one in-progress session per user.** A second
   `POST /v1/workouts` under a *different* `clientGeneratedId` while the caller
   has a row with `ended_at IS NULL` is `409 workout-in-progress-exists`, and
   the problem+json body carries **no workout id** (no extension member) — the
   client recovers via `GET /v1/workouts/active`, which returns that one row, or
   `404` when there is none. A concurrent burst of creates for a user with no
   active session admits exactly one row.
5. **The replay outcome wins over the in-progress conflict, and `409` has one
   source.** A retried create of the *same* `clientGeneratedId` returns `200`
   and never `409`, including when the first attempt is what created the
   in-progress workout and including when the two attempts race.
   `409 workout-in-progress-exists` is raised **only** when SQLSTATE `23505`
   on constraint `workout_user_active_key` — mapped after the failed
   statement's rollback — is followed by a re-read on
   `(user_id, client_generated_id)` that finds **nothing**; the same `23505`
   whose re-read finds a row is a replay and answers `200` with that stored
   row. A `23505` on any other constraint surfaces as
   `500`, not as the `409`. A zero-row insert whose re-read finds nothing (the
   stored row was concurrently deleted) retries the insert **once** and never
   answers `409`;
   if that retry also returns zero rows and the second re-read is also empty,
   the request fails as an internal error (`500`) rather than retrying again —
   the loop is bounded at one retry.
6. **Calendar fields are derived once, at write time, and never re-derived.**
   `local_date` is `localDateFor(startedAt, tzOffsetMinutes)` and both
   `local_date` and `tz_offset_minutes` are stored on the row. When
   `tzOffsetMinutes` is absent the server uses
   `offsetMinutesForZone(startedAt, user.timezone)` — the offset **at that
   instant**, so a workout started on a DST-transition day gets the offset in
   force then. `local_date` is unchanged by editing `title` / `notes`, by
   finishing, and by every read path. `localDate` is **never** client-settable:
   it is an unknown key on both `CreateWorkoutSchema` and `UpdateWorkoutSchema`,
   so `422` on `POST` and on `PATCH`. `tzOffsetMinutes` is accepted on `POST`
   only (it is how the offset is supplied, §5) and is `422` on `PATCH`.
7. **`startedAt` is bounded against clock skew.** A `startedAt` more than §6.4's
   +5 minutes in the future, or more than §6.4's 7 days in the past, measured
   against the server clock at write time, is `422 validation-error`; values
   inside both bounds are accepted. `endedAt` on the finish transition is bound
   by the same future window.
8. **Finish is a `PATCH`, and a finished workout is immutable.** `PATCH
   /v1/workouts/{id}` with `endedAt` set on an in-progress workout returns `200`
   with `endedAt` stored. `endedAt < startedAt` is `422`; `endedAt ===
   startedAt` is accepted. Against a **finished** workout every `PATCH` is
   `409 workout-finished` — including `PATCH {}` and `PATCH { endedAt: null }`
   — because the target's state is checked before the body is inspected.
   Against an **in-progress** workout, `PATCH {}` and `PATCH { endedAt: null }`
   are `200` no-ops returning the row unchanged. There is no
   `POST /v1/workouts/{id}/finish`.
9. **`workout-finished` is the single immutability signal, and `DELETE` is its
   one exemption.** `POST /v1/workouts/{id}/exercises`,
   `PATCH /v1/workout-exercises/{id}` and `DELETE /v1/workout-exercises/{id}`
   each return `409 workout-finished` when the (parent) workout has `ended_at`
   set. `DELETE /v1/workouts/{id}` succeeds with `204` on an in-progress **and**
   on a finished workout, hard-deletes, cascades to `workout_exercise`, and a
   repeat `DELETE` of the same id is `404`, not a second `204`.
10. **Add-exercise resolves through `findVisibleById` and snapshots the
    resolved row.** `POST /v1/workouts/{id}/exercises` returns `201` with
    `Location: /v1/workout-exercises/{id}` and a row whose
    `exercise_name_snapshot` / `modality_snapshot` equal the resolved
    exercise's `name` / `modality`; a later rename or soft-delete of that
    exercise leaves the snapshot unchanged. An `exerciseId` that
    `exerciseRepository.findVisibleById(actingUserId, …)` misses (absent, or
    another user's custom row) is `404 not-found`; an `isActive === false` row
    is `409 exercise-retired`. Adding the same `exerciseId` twice to one workout
    is allowed and creates a second row.
11. **Positions stay the dense sequence `0 … n-1`, and the range check is
    enforced below the schema.** Add without `position` appends at `n`; add with
    `0 ≤ position ≤ n` inserts and shifts the rows at and above it up by one;
    reorder with `0 ≤ position ≤ n-1` moves the row and renumbers the span
    between its old and new position; delete closes the gap. After each
    mutation the workout's positions are `0 … n-1` with no gap or duplicate, and
    `WorkoutDetail.exercises` is ordered by `position` ascending. A `position`
    above the allowed bound — including one above the `smallint` ceiling of
    32767, which must never reach the column as a Postgres `22003` `500` — is
    `422 validation-error`. A reorder to the row's current position is a `200`
    no-op.
12. **A reorder commits through a colliding intermediate state, and concurrent
    adds do not fail at `COMMIT`.** A two-row swap — the case whose
    intermediate state has two rows sharing a position — commits successfully
    through the repository's reorder path and leaves a dense `0 … n-1`
    sequence. N concurrent adds to the same workout all return `201`, none
    returns `500`, and the final sequence is dense with no duplicate: the
    deferred check at `COMMIT` sees only a valid final state.
13. **The per-workout advisory lock is its own statement, ahead of the count
    read.** In the transaction behind add, reorder and delete, the first
    statement that touches data — the first statement other than
    `SET CONSTRAINTS workout_exercise_position_key DEFERRED` — is
    `SELECT pg_advisory_xact_lock(hashtext($workoutId))`, and the `n` /
    `max(position)` read is a **separate, later** statement — the
    lock is never folded into a CTE alongside the read (§6.8; 03.2 D23's
    snapshot trap).
14. **The create path takes no advisory lock.** The create path issues no
    `pg_advisory_xact_lock` at all; its two
    guarantees come from `workout_user_client_id_key` and
    `workout_user_active_key` alone (§6.8).
15. **404, never 403, and a malformed id is also 404.** On every id-taking
    route — `GET`, `PATCH`, `DELETE /v1/workouts/{id}`,
    `POST /v1/workouts/{id}/exercises`, `PATCH` and
    `DELETE /v1/workout-exercises/{id}` — another user's row answers `404
    not-found`, indistinguishable from an absent id, and never `403`. A
    malformed `{id}` is `404` on all six, not `422`, because the branded guard
    runs in the repository before any query. A `workout_exercise` row's
    ownership is resolved through its parent workout. The add-exercise body's
    `exerciseId` follows the same rule: another user's custom exercise is `404`.
16. **Request bodies are closed and free text is bounded at ingress.** Every
    write body is `z.strictObject`, so `userId`, `id`, `localDate`, `source`,
    `tzOffsetMinutes` on `PATCH`, or either snapshot field is `422
    validation-error`, never silently dropped. `title` over
    `WORKOUT_TITLE_MAX` (120) or `notes` over `WORKOUT_NOTES_MAX` (4000) is
    `422` naming the field and **never `413 payload-too-large`**; an empty
    string is `422` while `null` and an omitted key are accepted; `title`
    rejects every C0 control character and DEL, `notes` rejects the same set
    except TAB, LF and CR; accepted text round-trips byte-for-byte, with no
    escaping or normalisation (`<script>` is a legal title).
17. **An account purge reaches these rows down both FK paths.** Deleting a
    `user` row removes that user's `workout` rows, the `workout_exercise` rows
    under them (`user → workout → workout_exercise`), **and** the
    `workout_exercise` rows that reference the user's custom `exercise` rows
    (`user → exercise → workout_exercise`) — in one `DELETE`, with no ordering
    step and no FK violation. No other user's `workout_exercise` row is touched.
18. **The four business log lines are emitted without free text.**
    `workout_started` is emitted on the fresh `201` create and **not** on the
    `200` idempotent replay; `workout_finished` is emitted on the finish
    transition only, not on a `PATCH` that edits `title` / `notes` and not on a
    `PATCH {}` no-op; `workout_deleted` and `workout_exercise_added` are emitted
    on their successes. No line, and no `422` problem+json `detail`, contains a
    `title` or `notes` value.
19. **The `@sin/core` additions land cleanly.** `WorkoutId` and
    `WorkoutExerciseId` ship the full `brandId()` quartet and a raw `string` is
    not assignable to either; `WORKOUT_SOURCE_VALUES` is frozen;
    `dto/workout.ts`'s seven schemas parse a valid payload and reject a missing
    required field and an unknown key; `time.ts`'s `localDateFor` and
    `offsetMinutesForZone` are pure and correct across a DST transition and a
    negative offset. Both new modules are re-exported from
    `packages/core/src/index.ts`, and every runtime export they add is listed
    **by name** in the `EXPECTED` array of
    `packages/core/scripts/check-exports.mjs`; `pnpm run core:purity` stays
    green and `dependencies` remains exactly `["zod"]`.
20. **All eight routes are authenticated and in the published contract.** Each
    returns `401` with no token and with an invalid token, each appears in
    `GET /openapi.json` with camelCase schemas derived from the Zod DTOs
    (including a declared response schema for both `204` deletes), and the CI
    OpenAPI drift check stays green.
21. **Docs stay consistent.** Every edit listed in §11's `DESIGN.md / doc
    edits` subsection is applied — or, for the §4.9 matrix row, confirmed
    unchanged against §6.5's `DELETE` semantics. `docs/specs/README.md`
    already carries the 05.0 / 05.1 / 05.2 roadmap rows, the repointed
    dependencies, the 05.2 ownership move and the `03.2 → Implemented` status
    fix: those were applied with this spec, not by the implementation PR,
    which verifies only the DESIGN.md half. §11 is the single source of the
    edit list; this criterion does not re-enumerate it.

---

## 3. Dependencies & exposed interface

### Needs

- **Spec 01** — `buildApp`, the RFC 9457 error contract, the auth plugin's
  `request.user`, `assertOwned` (`repositories/user.ts`), the UUIDv7
  convention, and the Testcontainers integration harness.
- **Spec 02** — `@sin/core`, `brandId()`, and the `MeSchema` DTO pattern.
- **Spec 03.0** — the Zod route-schema convention, the type provider, and the
  OpenAPI emit plus its drift check.
- **Spec 03.1** — the `exercise` table, `exerciseRepository.findVisibleById`,
  and `ExerciseId`.
- **Spec 03.2** — `ExerciseRetiredError` (`409 exercise-retired`), reused here
  for adding a retired exercise, and the `is_active = false` soft-delete
  semantics that make that state reachable.

No new runtime or dev dependencies.

### Provides — stable surface (Specs 05.1, 06, 07, 11 consume this)

| Export | Shape | Consumed by |
|---|---|---|
| `workout` and `workout_exercise` tables | Postgres tables (migration `0005`) | 05.1 (`set_entry` FKs to `workout_exercise`), 07 (history, PRs), 11 (export, purge) |
| `POST /v1/workouts`, `GET /v1/workouts/active`, `GET /v1/workouts/{id}`, `PATCH /v1/workouts/{id}`, `DELETE /v1/workouts/{id}`, `POST /v1/workouts/{id}/exercises`, `PATCH /v1/workout-exercises/{id}`, `DELETE /v1/workout-exercises/{id}` | eight endpoints | 06 (session start, resume, finish, exercise add/reorder/remove) |
| `WorkoutSchema`, `WorkoutExerciseSchema`, `WorkoutDetailSchema`, `CreateWorkoutSchema`, `UpdateWorkoutSchema`, `AddWorkoutExerciseSchema`, `UpdateWorkoutExerciseSchema` (+ inferred types), `WorkoutId`, `WorkoutExerciseId`, `WORKOUT_SOURCE_VALUES` (`@sin/core`) | Zod DTOs, branded ids, frozen constant | 05.1 (set DTOs build on the ids), 06 (form validation, client types) |
| `localDateFor`, `offsetMinutesForZone` (`@sin/core`, new `time.ts`) | pure functions | 05.1 (any calendar-derived field), 07 (history bucketing) |
| `workoutRepository` (`apps/api/src/repositories/workout.ts`) | repository interface + Prisma factory | 05.1 (set writes reuse its ownership checks), 07 (history reads, PR recompute) |

**Consumers must note:**
- The purge-ordering obligation that 03.1 §3 and 03.2 §3 hand to Spec 05 —
  `workout_exercise.exercise_id` versus `exercise.owner_user_id ON DELETE
  CASCADE` — is **discharged here**: `workout_exercise.exercise_id` is
  `ON DELETE CASCADE` (§4), so an account purge needs no ordering step and
  cannot fail on it (§4, D37).
- 05.1 extends the finish transition with the working-set integrity rule, and
  07 adds the PR recompute to the same transaction. Neither is built here;
  §6 names the seam for each.
- 05.0 ships no write throttle. 03.2's D20 and D21 stay open until 05.2.

---

## 4. Data model

Owned by this spec: **`workout`** and **`workout_exercise`**. Migration
**`0005_create_workout_session`** — additive, expand-only. The migration SQL is
**hand-authored**: the partial unique index `WHERE ended_at IS NULL`, the two
`CHECK (… IN (…))` literal lists, and the `DEFERRABLE` unique constraint are not
expressible in the Prisma schema DSL, so the `.sql` file is the source of truth
and the Prisma models exist for the client only (the same raw-SQL carve-out
Spec 03.1 §4 used for `0002`). The file follows the `0002` conventions: a
`NNNN_snake_case` directory, a header comment naming this section, explicit
`TIMESTAMPTZ(6)` column types, and a "Down = test teardown / local reset only,
never production" comment. Production undo is `git revert` plus a forward
migration, never a down script (Spec 01 §11).

```
Table: workout
  id                  uuid        PK, app-generated UUIDv7
  user_id             uuid        NOT NULL FK → user(id) ON DELETE CASCADE
  title               text        NULL
  notes               text        NULL
  started_at          timestamptz NOT NULL
  ended_at            timestamptz NULL            -- NULL = in progress
  local_date          date        NOT NULL
  tz_offset_minutes   smallint    NOT NULL
  client_generated_id uuid        NOT NULL        -- idempotency key
  source              text        NOT NULL DEFAULT 'manual'
                                  CHECK (source IN (<@sin/core WORKOUT_SOURCE_VALUES>))
  created_at/updated_at timestamptz NOT NULL      -- app-stamped, no trigger

Indexes:
  workout_user_client_id_key  UNIQUE (user_id, client_generated_id)
  workout_user_started_idx    (user_id, started_at DESC)   -- the 07 history list
  workout_user_active_key     UNIQUE (user_id) WHERE ended_at IS NULL

Table: workout_exercise
  id                     uuid        PK, UUIDv7
  workout_id             uuid        NOT NULL FK → workout(id) ON DELETE CASCADE
  position               smallint    NOT NULL
  exercise_id            uuid        NOT NULL FK → exercise(id) ON DELETE CASCADE
  exercise_name_snapshot text        NOT NULL
  modality_snapshot      text        NOT NULL
                                     CHECK (modality_snapshot IN (<MODALITY_VALUES>))
  notes                  text        NULL
  created_at/updated_at  timestamptz NOT NULL

Indexes:
  workout_exercise_position_key UNIQUE (workout_id, position) DEFERRABLE INITIALLY IMMEDIATE
  workout_exercise_exercise_idx (exercise_id)
```

- **`exercise_id` is `ON DELETE CASCADE`, which discharges the purge-ordering
  obligation.** Spec 03.1 §3 and Spec 03.2 §3 both hand Spec 05 the question of
  how an account purge orders `workout_exercise` against the user's custom
  `exercise` rows. `exercise.owner_user_id` is itself `ON DELETE CASCADE`
  (03.1 §4), so purging a user deletes their custom exercises. A `RESTRICT` on
  `workout_exercise.exercise_id` would refuse the delete the moment the cascade
  reached the exercise row, and a `NO ACTION` would only pass by coincidence —
  its end-of-statement check happens to find no referencing rows left because
  the other cascade removed them first, which is a property of how this
  particular purge is written, not a guarantee. With `CASCADE` on both paths
  (`user → workout → workout_exercise`
  and `user → exercise → workout_exercise`) no ordering is needed. The cascade
  can only ever fire for the purged user's own rows: global rows are never
  hard-deleted (DESIGN §4.2, append-only, retire by `is_active = false`), so no
  other user's history is reachable through it. `workout_exercise_exercise_idx`
  exists for exactly this delete path — without it, each exercise delete would
  sequentially scan `workout_exercise` to find referencing rows — and doubles as
  the lookup for "which workouts used this exercise" (Spec 07).
- **The snapshots are `NOT NULL` and written at add time.**
  `exercise_name_snapshot` and `modality_snapshot` are copied from
  `exerciseRepository.findVisibleById` inside the add-exercise write (§6), so a
  later rename, soft-delete or fork of the exercise never rewrites a logged
  session (DESIGN §4.4). They are `NOT NULL` because a snapshot that can be
  absent is a second code path every history reader must handle; the add path
  is the only writer and always has the row in hand. The `modality_snapshot`
  `CHECK` renders `MODALITY_VALUES` into the migration exactly as 03.1 AC2 does
  for `exercise.modality`, and inherits its drift guard: a unit test asserts
  the migration `.sql` **contains** the rendered clause (string containment, not
  a parse of `pg_get_constraintdef()`, which Postgres normalizes to
  `= ANY (ARRAY[...])`). The same applies to `source` against
  `WORKOUT_SOURCE_VALUES`.
- **`superset_group` and `routine_id` are deliberately omitted.** DESIGN §4.4
  lists both on `workout` / `workout_exercise`, but nothing in v1 M1 writes or
  reads them. Spec 09 adds `superset_group` additively, alongside
  `routine_item.superset_group`. `routine_id` waits because the `routine` table
  does not exist until 09, and a nullable `uuid` with no FK now and an FK later
  is worse than one additive `ALTER TABLE … ADD COLUMN … REFERENCES` in 09.
  Both omissions are recorded as decisions in §12 so a reader does not conclude
  DESIGN was missed.
- **No `change_xid`-style trigger.** `created_at` / `updated_at` stay
  app-stamped, as for `user` (Spec 01) and `exercise` (03.1 §4). The Spec 03.3
  trigger is a narrow exception whose only purpose is sync-correctness for a
  catalog that clients delta-pull; workouts have no delta-sync contract in v1,
  so a trigger would add write-path cost and a second stamping mechanism for no
  reader.
- **The three `workout` indexes each serve one rule.**
  `workout_user_client_id_key` is the idempotency key (DESIGN §6): a retried
  `POST /v1/workouts` collides on it and the handler replays the stored row
  (§6). It is scoped by `user_id` so one user's key can never leak or collide
  with another's. `workout_user_active_key` is a **partial** unique index on
  `user_id WHERE ended_at IS NULL`, which makes "at most one in-progress
  session per user" a database invariant rather than a check-then-insert race;
  it also serves the `GET /v1/workouts/active` lookup.
  `workout_user_started_idx` `(user_id, started_at DESC)` is the history-list
  access path that Spec 07 will read; it is created here, with the table, so
  07 needs no index build on a populated table.
- **`workout_exercise_position_key` is `DEFERRABLE INITIALLY IMMEDIATE`.** A
  reorder moves several rows' `position` values inside one transaction, and
  intermediate states (two rows briefly sharing a position) are unavoidable when
  swapping. A plain unique constraint checks each row as it is written and
  would reject the swap; a deferrable one lets the reorder transaction run
  `SET CONSTRAINTS workout_exercise_position_key DEFERRED` and have the check
  happen at commit. `INITIALLY IMMEDIATE` keeps ordinary inserts and deletes
  strict, so only the reorder path opts in. Note this must be a
  **constraint**, not a bare `CREATE UNIQUE INDEX`, because Postgres cannot
  defer a unique index.
- **The `User` and `Exercise` models gain back-references.** Prisma requires
  both sides of a relation, so `workout.user_id → user(id)` obliges a
  `workouts Workout[]` field on `User` (owned by Spec 01) and
  `workout_exercise.exercise_id → exercise(id)` obliges a
  `workoutExercises WorkoutExercise[]` field on `Exercise` (owned by 03.1), or
  `prisma validate` fails in CI — the same trap 03.1 §4 recorded for
  `User.customExercises`. The `Workout` and `WorkoutExercise` models also need
  `@@map("workout")` / `@@map("workout_exercise")` and per-field `@map`
  snake_case mappings so the client's camelCase fields line up with the
  hand-authored columns. All of this is a **Prisma-client-only change** — no
  column, no extra migration — and does not otherwise disturb Spec 01's or
  03.1's ownership of `user` and `exercise`.

---

## 5. API surface

All eight routes are under `/v1`, require authentication, and go through the
Spec 03.0 route-schema convention. Every route also has the two ambient
failures, `401 unauthenticated` / `401 invalid-token` (Spec 01 §5), listed once
per route below so the block is self-contained; `413 payload-too-large` and
`415 unsupported-media-type` come from the app-wide body parser and are not
repeated.

```
POST /v1/workouts                                 auth required
  201  Workout                                     Location: /v1/workouts/{id}
  200  Workout  — idempotent replay: (userId, clientGeneratedId) already
                  stored; the stored row is returned, the body is ignored (§6)
  409  workout-in-progress-exists    — caller already has an unfinished workout
                                       under a *different* clientGeneratedId
  422  validation-error              — malformed body, or startedAt outside
                                       the clock-skew window (§6)
  401  unauthenticated / invalid-token

GET /v1/workouts/active                           auth required
  200  WorkoutDetail                 — the caller's one in-progress workout
  404  not-found                     — the caller has no in-progress workout
  401  unauthenticated / invalid-token

GET /v1/workouts/{id}                             auth required
  200  WorkoutDetail                 — in progress or finished
  404  not-found                     — absent, or owned by a different user
  401  unauthenticated / invalid-token

PATCH /v1/workouts/{id}                           auth required
  200  Workout                       — { title?, notes?, endedAt? }; setting
                                       endedAt is the finish transition (§6)
  404  not-found                     — absent, or owned by a different user
  409  workout-finished              — the workout already has endedAt set;
                                       no field is editable, and endedAt
                                       cannot be cleared (§6)
  422  validation-error              — malformed body, or endedAt < startedAt
  401  unauthenticated / invalid-token

DELETE /v1/workouts/{id}                          auth required
  204  (no body) — hard delete; cascades to workout_exercise; allowed on an
                   in-progress or a finished workout
  404  not-found                     — absent, or owned by a different user
  401  unauthenticated / invalid-token

POST /v1/workouts/{id}/exercises                  auth required
  201  WorkoutExercise                             Location:
                                       /v1/workout-exercises/{id}
  404  not-found                     — the workout is absent or owned by a
                                       different user; or exerciseId is not
                                       visible to the caller (absent, or another
                                       user's custom exercise)
  409  workout-finished              — the workout already has endedAt set
  409  exercise-retired              — the exercise has isActive=false
  422  validation-error
  401  unauthenticated / invalid-token

PATCH /v1/workout-exercises/{id}                  auth required
  200  WorkoutExercise               — { position?, notes? }
  404  not-found                     — absent, or its workout is owned by a
                                       different user
  409  workout-finished              — the parent workout has endedAt set
  422  validation-error
  401  unauthenticated / invalid-token

DELETE /v1/workout-exercises/{id}                 auth required
  204  (no body)
  404  not-found                     — absent, or its workout is owned by a
                                       different user
  409  workout-finished              — the parent workout has endedAt set
  401  unauthenticated / invalid-token
```

**The `Location` of a new workout exercise has no `GET` in 05.0.**
`/v1/workout-exercises/{id}` is the canonical id-bearing URL for the created
row and is where `PATCH` and `DELETE` address it, but the resource is read
through `GET /v1/workouts/{id}`; a `GET` on the singular path can be added
additively if 05.1 or 06 wants one.

**Path ids.** Every `{id}` path parameter is declared as
`z.object({ id: z.string() })`, exactly as `routes/exercises.ts` declares
`exerciseIdParams`, and the repository turns a malformed id into
`NotFoundError` with the branded `isWorkoutId` / `isWorkoutExerciseId` guard
before any query (as `exercise.prisma.ts` does with `isExerciseId`). A
malformed `{id}` is therefore `404 not-found`, not `422`, on every route above;
this follows 03.1 / 03.2, whose blocks list no `422` for a bad id. A malformed
`exerciseId` in the `POST .../exercises` **body** is different: it is body
validation, so `422 validation-error`.

Three things about this surface are pinned down here so a later spec does not
have to guess them.

- **There is no paginated `GET /v1/workouts`.** The history list is Spec 07's;
  05.0 ships only the reads the logging screen needs. `GET /v1/workouts/active`
  exists because Spec 06 must be able to **resume** an in-progress session: the
  cold-load redirect that Q12 and Spec 04.0 §12 make routine can drop a user
  onto a fresh page with no client state, and the client then needs "the
  workout I was in the middle of" without knowing its id. The partial unique
  index `workout_user_active_key` (§4) makes that well defined: at most one row
  can match, so the endpoint returns `200` with that row or `404`, never a
  list and never an ambiguity. The `409 workout-in-progress-exists` body
  carries no workout id (standard problem+json, no extension member), so the
  client's recovery from that `409` is the same call — `GET /v1/workouts/active`
  — and then "resume or discard". This spares Spec 01's problem+json contract
  an extension-member mechanism that one error would be the only user of.
- **404, never 403, for another user's row.** A workout that exists but belongs
  to someone else is indistinguishable, on the wire, from one that does not
  exist — the 03.1 / 03.2 convention, so a caller cannot probe for other users'
  ids. The repository enforces it: `workout` rows are `{ userId }`-shaped, so
  the ownership check is `assertOwned` (`apps/api/src/repositories/user.ts`),
  called with the acting user's id. A `workout_exercise` row carries no
  `user_id`; its ownership is its parent workout's, so the repository resolves
  it through the join and applies the same 404 (§6, Wiring points).
- **Schemas.** Response schemas are `z.object` (not `.strict()`), so the
  serializer treats each as a field **allowlist** (Spec 03.0 §6.5): a column
  added later is not exposed until a DTO opts it in. Request bodies are
  `z.strictObject`, so an unknown key is `422` rather than silently dropped —
  which is also what stops a client from smuggling `userId`, `localDate` or
  `source` into a write. Only the **success** response is declared per route
  (Spec 03.1 §5): `201` and `200` for `POST /v1/workouts` (both `Workout`),
  `200` for the rest, and `204` for the two deletes. Error bodies come from the
  shared problem+json contract, registered on both the root and `/v1` scopes.

### `@sin/core` DTOs

Wire fields are camelCase, the camelCase of the §4 column names, with the API
mapping to and from the snake_case columns. Neither row DTO carries a
**`userId`**: every row a caller can read is the caller's own, so the field
would carry no information (unlike `Exercise.ownerUserId`, where `null` means
"global"), and `workout_exercise` has no such column. Timestamps are
`z.iso.datetime({ offset: true })`; `localDate` is `z.iso.date()`; ids are the
branded schemas, never bare `z.string()`.

**Naming convention for 05.0's additions: `XxxSchema` for the Zod value, bare
`Xxx` for the `z.infer` type, and no `Body` / `Response` suffix.** The core
package carries two styles today: 03.1 and 03.2 use `ExerciseSchema` /
`Exercise` and `CreateExerciseSchema` / `CreateExercise`; Spec 02 uses
`MeSchema` / `Me` but names its update type `UpdateMeInput`; 03.3 introduced
suffix-less values (`ExercisesResponse`, `MuscleGroupsResponse`) with
`Body`-suffixed types (`ExercisesResponseBody`). The evidence for `Schema` is a
majority, not unanimity. It wins for three reasons. It is the existing majority
and what the branded ids already use (`UserIdSchema`, `ExerciseIdSchema`). It
makes the runtime value and the compile-time type distinct at a glance:
TypeScript would allow a const and a type to share one name (03.3's values could
have done so), but `WorkoutSchema.parse(x)` versus `Workout` is clearer than a
same-named pair whose meaning depends on the position it is used in. And it
needs no per-type suffix decision: the type is the bare noun. 03.3's shipped
exports are **not** renamed here, since that is churn on a merged wire contract
for no behavioral gain; if it is ever wanted it is its own change. The choice
is recorded as a decision in §12.

```ts
// packages/core/src/ids.ts — additions (the brandId() quartet, once per id)
const workoutId = brandId("WorkoutId");
export const WorkoutIdSchema = workoutId.schema;
export type WorkoutId = z.infer<typeof WorkoutIdSchema>;
export const parseWorkoutId = workoutId.parse;
export const isWorkoutId = workoutId.is;

const workoutExerciseId = brandId("WorkoutExerciseId");
export const WorkoutExerciseIdSchema = workoutExerciseId.schema;
export type WorkoutExerciseId = z.infer<typeof WorkoutExerciseIdSchema>;
export const parseWorkoutExerciseId = workoutExerciseId.parse;
export const isWorkoutExerciseId = workoutExerciseId.is;

// packages/core/src/enums.ts — addition. Renders the workout.source CHECK (§4).
export const WORKOUT_SOURCE_VALUES = Object.freeze(["manual"] as const);
export type WorkoutSource = (typeof WORKOUT_SOURCE_VALUES)[number];

// packages/core/src/dto/workout.ts — new module
import { z } from "zod";
import { MODALITY_VALUES, WORKOUT_SOURCE_VALUES } from "../enums.js";
import { ExerciseIdSchema, WorkoutExerciseIdSchema, WorkoutIdSchema } from "../ids.js";
import { noControlChars } from "./exercise.js";

// Bounds for the two free-text fields (§7 argues them). Code constants, not
// env config (§8). Worst case 4000 chars * 4 bytes is 16 KB, well inside the
// 64 KiB BODY_LIMIT_BYTES, so an over-long field is a 422, never a 413.
export const WORKOUT_TITLE_MAX = 120;
export const WORKOUT_NOTES_MAX = 4000;

// Like noControlChars, but permits TAB, LF and CR: notes are multi-line prose,
// where a title is a single line. Still rejects every other C0 char and DEL.
export const noControlCharsExceptWhitespace = (s: string): boolean => { /* ... */ };

const WorkoutTitle = z.string().min(1).max(WORKOUT_TITLE_MAX).refine(noControlChars, "control characters not allowed");
const WorkoutNotes = z.string().min(1).max(WORKOUT_NOTES_MAX).refine(noControlCharsExceptWhitespace, "control characters not allowed");

// tz_offset_minutes is a smallint; real-world offsets run UTC-12:00..UTC+14:00.
const TzOffsetMinutes = z.number().int().min(-720).max(840);

/** One workout session. Responses are z.object: a field allowlist (03.0 §6.5). */
export const WorkoutSchema = z.object({
  id: WorkoutIdSchema,
  title: z.string().nullable(),
  notes: z.string().nullable(),
  startedAt: z.iso.datetime({ offset: true }),
  endedAt: z.iso.datetime({ offset: true }).nullable(), // null = in progress
  localDate: z.iso.date(),                              // "YYYY-MM-DD", never re-derived (§6)
  tzOffsetMinutes: TzOffsetMinutes,
  clientGeneratedId: z.guid(),                          // idempotency key, echoed back
  source: z.enum(WORKOUT_SOURCE_VALUES),
  createdAt: z.iso.datetime({ offset: true }),
  updatedAt: z.iso.datetime({ offset: true }),
});
export type Workout = z.infer<typeof WorkoutSchema>;

/** One exercise within a workout. Snapshots are copied at add time (§4, §6). */
export const WorkoutExerciseSchema = z.object({
  id: WorkoutExerciseIdSchema,
  workoutId: WorkoutIdSchema,
  position: z.number().int().min(0),                    // zero-based (§6)
  exerciseId: ExerciseIdSchema,
  exerciseNameSnapshot: z.string(),
  modalitySnapshot: z.enum(MODALITY_VALUES),
  notes: z.string().nullable(),
  createdAt: z.iso.datetime({ offset: true }),
  updatedAt: z.iso.datetime({ offset: true }),
});
export type WorkoutExercise = z.infer<typeof WorkoutExerciseSchema>;

/**
 * GET /v1/workouts/active and GET /v1/workouts/{id}: the workout plus its
 * exercises, ordered by position ascending. Spec 05.1 adds each exercise's sets
 * here, additively.
 */
export const WorkoutDetailSchema = WorkoutSchema.extend({
  exercises: z.array(WorkoutExerciseSchema),
});
export type WorkoutDetail = z.infer<typeof WorkoutDetailSchema>;

/** POST /v1/workouts body. `source` is not client-settable in v1. */
export const CreateWorkoutSchema = z.strictObject({
  clientGeneratedId: z.guid(),
  startedAt: z.iso.datetime({ offset: true }),
  tzOffsetMinutes: TzOffsetMinutes.optional(),          // absent -> user.timezone (§6)
  title: WorkoutTitle.nullable().optional(),
  notes: WorkoutNotes.nullable().optional(),
});
export type CreateWorkout = z.infer<typeof CreateWorkoutSchema>;

/**
 * PATCH /v1/workouts/{id} body. `endedAt` is the finish transition. `null` is
 * accepted so that "un-finish" reaches the handler and gets the 409 it is owed
 * on a finished workout rather than a 422 (§6); on an in-progress workout it is
 * a no-op.
 */
export const UpdateWorkoutSchema = z.strictObject({
  title: WorkoutTitle.nullable().optional(),
  notes: WorkoutNotes.nullable().optional(),
  endedAt: z.iso.datetime({ offset: true }).nullable().optional(),
});
export type UpdateWorkout = z.infer<typeof UpdateWorkoutSchema>;

/** POST /v1/workouts/{id}/exercises body. Absent position = append (§6). */
export const AddWorkoutExerciseSchema = z.strictObject({
  exerciseId: ExerciseIdSchema,
  position: z.number().int().min(0).optional(),
  notes: WorkoutNotes.nullable().optional(),
});
export type AddWorkoutExercise = z.infer<typeof AddWorkoutExerciseSchema>;

/** PATCH /v1/workout-exercises/{id} body: reorder and/or edit notes. */
export const UpdateWorkoutExerciseSchema = z.strictObject({
  position: z.number().int().min(0).optional(),
  notes: WorkoutNotes.nullable().optional(),
});
export type UpdateWorkoutExercise = z.infer<typeof UpdateWorkoutExerciseSchema>;
```

Points on the listing that are choices, not accidents:

- **`clientGeneratedId` is `z.guid()`, not a branded id.** It is a client-minted
  idempotency key, not the identity of a resource the server hands out, so
  branding it would add a type nobody passes between functions. It uses
  `z.guid()` for the same reason `brandId()` does (`ids.ts`): the lenient
  8-4-4-4-12 shape, so a client's v4 or v7 both pass and Postgres's `uuid`
  column accepts either.
- **`startedAt` and `endedAt` keep the offset the client sent**
  (`offset: true`). The instant is what is stored; `local_date` is derived
  from `tzOffsetMinutes`, not from the offset embedded in the string (§6), so
  the two cannot disagree in a way that matters.
- **The clock-skew window on `startedAt`, the `endedAt >= startedAt` rule, and
  every rule that needs the stored row** cannot be Zod refinements: a schema
  sees only the body, not the server clock or the database. They are handler
  rules that raise `ValidationError` (`422`); §6 states them.
- **`UpdateWorkoutSchema` and `UpdateWorkoutExerciseSchema` are not
  `.refine`d to "at least one field".** `PATCH {}` is a valid no-op that returns
  the row unchanged, which is also how a client fetches a fresh `updatedAt`.
- **`position` is a zero-based integer.** Range checks against the actual
  number of exercises, and the renumbering, are §6's; the schema only rejects
  negatives and non-integers.

### New `AppError` subclasses

`apps/api/src/errors/app-error.ts` gains two classes, in the existing style —
`status` / `slug` / `title` / `publicDetail` as `readonly` fields, and a
constructor whose defaulted argument is the **internal** reason (server logs
only, never the response body):

```ts
export class WorkoutFinishedError extends AppError {
  readonly status = 409;
  readonly slug = "workout-finished";
  readonly title = "Workout finished";
  readonly publicDetail =
    "This workout has already been finished and can no longer be modified.";

  constructor(internal = "target workout has ended_at set") {
    super(internal);
  }
}

export class WorkoutInProgressExistsError extends AppError {
  readonly status = 409;
  readonly slug = "workout-in-progress-exists";
  readonly title = "Workout already in progress";
  readonly publicDetail =
    "You already have a workout in progress. Resume or discard it first.";

  constructor(internal = "workout_user_active_key: caller already has ended_at IS NULL") {
    super(internal);
  }
}
```

`NotFoundError` and `ValidationError` are **reused**, not subclassed. So is
`ExerciseRetiredError` (`409 exercise-retired`, from 03.2) for adding an
`isActive = false` exercise; its `publicDetail` reads "can no longer be
modified", which is loose for an add but accurate enough that a third exercise
error is not worth it. The house style is **one class per business rule**: the
slug is the contract a client switches on, so `workout-finished` and
`workout-in-progress-exists` are distinct classes even though both are `409`.
There is no generic `ConflictError` carrying a slug string.

`workout-finished` is deliberately the single "the parent is immutable" signal:
it is returned by `PATCH /v1/workouts/{id}` and by all three
exercise-within-workout writes against a finished workout, so a client has one
rule to handle. `DELETE /v1/workouts/{id}` is the exception and is always
allowed (DESIGN §4.5, §4.9).

### `time.ts` — the one place `local_date` is derived

`packages/core/src/time.ts` (new) holds two pure functions.
`localDateFor(instantIso, offsetMinutes)` shifts the instant by the offset and
returns the calendar date as `"YYYY-MM-DD"` — plain arithmetic, no `Intl`.
`offsetMinutesForZone(instantIso, ianaTimeZone)` returns the zone's UTC offset
in minutes **at that instant** (so daylight saving is honored), using
`Intl.DateTimeFormat` with `timeZoneName: "longOffset"` and parsing the
resulting `GMT±HH:MM` (bare `GMT` means `0`). `Intl` is ECMA-402, a language
built-in rather than a Node API, and `dto/me.ts`'s `isValidTimeZone` already
depends on it, so it passes the `core:purity` check. These two are the only
code that derives `local_date`; the route handler calls them and nothing else
does (DESIGN §4.0, and §6 below). Both new modules must be re-exported from
`packages/core/src/index.ts`, and **every runtime export they add** —
`WorkoutIdSchema`, `parseWorkoutId`, `isWorkoutId`, the `WorkoutExerciseId`
trio, `WORKOUT_SOURCE_VALUES`, `noControlCharsExceptWhitespace`,
`WORKOUT_TITLE_MAX`, `WORKOUT_NOTES_MAX`, the seven DTO schemas,
`localDateFor` and `offsetMinutesForZone` — added **by name** to the
hand-maintained `EXPECTED` array in
`packages/core/scripts/check-exports.mjs`, which fails the build when a listed
name is missing from the built barrel.
There is no new runtime dependency: `test/deps.test.ts` pins `dependencies` to
exactly `["zod"]`, and nothing here needs a date library.

---

## 6. Behavior & logic

Eight rules carry the weight of this spec. Each is stated so it can be
asserted on its own; §2's acceptance criteria are drawn from them.

Two structural conventions apply throughout and are not repeated per rule.
First, **every query goes through the repository** (DESIGN R8) and, by the
03.1 / 03.2 house convention, **every repository method takes `actingUserId`
as its first parameter**; no handler ever queries by id alone. Second, **a
malformed `{id}` path parameter is `404 not-found`, never `422`**: the
repository runs the branded guard (`isWorkoutId` / `isWorkoutExerciseId`,
spec'd in §5) before it issues any query and throws `NotFoundError` on a miss,
exactly as
`loadVisibleRow` in `apps/api/src/repositories/exercise.prisma.ts` does with
`isExerciseId`.

### 6.1 Idempotent create

`POST /v1/workouts` writes with `ON CONFLICT … DO NOTHING` on the idempotency
key and re-reads on a miss:

```sql
INSERT INTO workout (...)
VALUES (...)
ON CONFLICT (user_id, client_generated_id) DO NOTHING
RETURNING *;
```

A returned row is a fresh create ⇒ `201` plus a `Location` header. **Zero rows
⇒ re-read by `(user_id, client_generated_id)` and return the stored row with
`200`** and no `Location` header. Spec 03.1 §6.3 recorded the caveat that
`ON CONFLICT` needs a real unique index to infer against;
`workout_user_client_id_key` (§4) is that index, and it is scoped by `user_id`
so one user's key can never arbitrate against another's row.

**A replay whose body differs from the stored row returns the stored row
unchanged.** The body is ignored, not compared: DESIGN §6 says "replaying the
same id returns the existing resource, never a duplicate", and the only client
that sends a differing body is a buggy one or a retry after a local edit. A
`409` on a body mismatch would turn the one case idempotency exists to
survive — a retry over a flaky gym connection whose first attempt actually
succeeded — into a hard error the client cannot distinguish from a real
conflict. The `200`-versus-`201` split is the entire signal: a client that
cares whether it created the row reads the status code.

**The body is still validated like any other create.**
`CreateWorkoutSchema` and the §6.4 skew bounds run **before** the insert, so a
replay that arrives more than seven days after its `startedAt` is `422` by
design rather than a `200` replay. That ordering is deliberate and is not to
be rearranged: validation is a property of the request, idempotency a property
of the row, and a request that would not have been accepted the first time is
not made acceptable by having been sent before.

The create is a **single statement**, issued outside any interactive
transaction, so a `23505` rolls back exactly that statement and the re-read
(§6.2) runs on a clean connection.

The `POST` body is `z.strictObject`, so `source`, `localDate`, `userId` and
`id` are rejected as unknown keys rather than silently dropped (§5).
`source` is server-set to `'manual'` in v1.

### 6.2 One in-progress session per user, and its interaction with replay

A user may have at most one workout with `ended_at IS NULL`. This is enforced
by the partial unique index `workout_user_active_key` (§4), not by a
check-then-insert in the handler, so two concurrent creates cannot both pass a
"do you already have one?" read and both insert. A second create while one is
in progress is `409 workout-in-progress-exists`
(`WorkoutInProgressExistsError`, §5).

**The `409` body carries no workout id.** The Spec 01 problem+json contract
has no extension-member mechanism and this error would be its only user, so
the client recovers the active workout by calling `GET /v1/workouts/active`
and then offers "resume or discard" (§5). That is one extra round trip on a
path the user is already stalled on, against permanently widening the
error contract.

**The replay outcome wins over the in-progress conflict, and the re-read is
what guarantees it.** `ON CONFLICT (user_id, client_generated_id) DO NOTHING`
swallows a violation of the **arbiter** index only. A violation of the
non-arbiter partial unique index `workout_user_active_key` is *not* swallowed:
it raises SQLSTATE `23505` and aborts the statement. That gives the statement
three possible outcomes, and the `23505` one is resolved by a second query
rather than answered directly:

1. Run the `ON CONFLICT (user_id, client_generated_id) DO NOTHING` insert.
2. **A row returned** ⇒ fresh create ⇒ `201` plus `Location`.
3. **Zero rows** ⇒ a row with this `(user_id, client_generated_id)` already
   existed ⇒ re-read by that key and return it with `200`. A re-read that
   finds nothing means the stored row was concurrently deleted between the
   insert and the re-read; **retry the insert once**, and never `409` on that
   path. If that retry also returns zero rows and the second re-read is also
   empty, the request fails as an internal error (`500`): the loop is bounded
   at one retry and never degrades into a spin.
4. **SQLSTATE `23505` on constraint `workout_user_active_key`** ⇒ the caller
   already has an unfinished workout — *or* this request is a same-key replay
   that raced its own original. The statement has aborted the transaction, so
   the mapping happens after the rollback, outside the failed statement; the
   handler then **re-reads by `(user_id, client_generated_id)`**. A hit is a
   replay ⇒ `200` with the stored row and no `Location`. A miss ⇒ `409
   workout-in-progress-exists`. A `23505` on any other constraint is a bug,
   not a business rule, and must surface as `500` rather than be folded into
   the `409`.

**Why the `23505` branch re-reads instead of answering `409` directly (D50).**
It is tempting to argue that a same-key retry can never reach this branch,
because `ON CONFLICT`'s pre-check looks at the arbiter index first and a
conflict there abandons the tuple. That holds only when the conflicting row is
already committed, or is in flight and visible to the pre-check's dirty
snapshot (which waits on it and re-checks). It does **not** hold when a rival
transaction has not yet inserted its own arbiter index tuple: both requests
then pass the pre-check and speculate, and the loser resolves its conflict on
whichever unique index it reaches first — index tuples are inserted in
index-OID order, so the answer would depend on the order the two `CREATE`
statements happen to appear in the hand-authored `0005` migration. Making the
`409` branch re-read by the idempotency key removes that dependency
altogether: the outcome is decided by data, not by index order, at the cost of
one `SELECT` on an error path.

**A retried create of the *same* `clientGeneratedId` therefore returns `200`,
never `409`**, even when the first attempt is what created the in-progress
workout the partial index objects to, and even when the retry races that first
attempt. Two mechanisms deliver that, in this order: the arbiter conflict ends
the ordinary (already-committed) replay at step 3, and the re-read at step 4
catches the racing replay that the arbiter pre-check could not see. Neither is
a property of handler ordering that a refactor can invert, and the guarantee
survives a change of index-creation order — but it *would* break if the
arbiter were changed or the `ON CONFLICT` clause dropped, which is why the
clause and the index are described together here and in §4.

### 6.3 Calendar fields

The client sends `startedAt` (its own device clock, with offset) and
`tzOffsetMinutes` (its current UTC offset). The server derives `local_date`
once, at write time, by calling `localDateFor(startedAt, tzOffsetMinutes)`
(`packages/core/src/time.ts`, §5) and stores both `local_date` and
`tz_offset_minutes` on the row.

When `tzOffsetMinutes` is absent, the server falls back to the user's stored
`user.timezone` via `offsetMinutesForZone(startedAt, user.timezone)` — the
offset **at that instant**, so a workout started the morning a DST transition
lands gets the offset in force then, not the one in force at read time
(DESIGN §6).

`local_date` is **never re-derived from UTC afterwards** (DESIGN §4.0). It is
not recomputed when `startedAt` changes, because `startedAt` is not editable
(§5: `UpdateWorkoutSchema` has no `startedAt`); it is not recomputed on
finish; and no read path derives it. This is the whole point of storing it: a
calendar query ("this week", a streak, the month view in Spec 07) reads the
column, and re-deriving would silently move a late-evening session to the next
day for anyone east of UTC.

`local_date` is **never client-settable** — `localDate` appears in neither
`CreateWorkoutSchema` nor `UpdateWorkoutSchema`. `tz_offset_minutes` is
settable on create only: `tzOffsetMinutes` is an optional field of
`CreateWorkoutSchema` (it is how the client supplies the offset, §5) and
appears in no other body. Both bodies are `z.strictObject`, so sending
`localDate` on either, or `tzOffsetMinutes` on the `PATCH`, is `422`.

### 6.4 Clock-skew bound on `startedAt`

`startedAt` is rejected with `422 validation-error` when it is more than
**5 minutes in the future** or more than **7 days in the past**, measured
against the server clock at the moment of the write. `endedAt`, on the finish
transition, is bound by the same 5-minute future window.

These cannot be Zod refinements — a schema sees the body, not the server
clock — so they are handler rules raising `ValidationError`, with the two
bounds as code constants in `packages/core/src/dto/workout.ts` alongside
`WORKOUT_TITLE_MAX` (§8: no env config, the `MAX_CUSTOM_EXERCISES_PER_USER`
precedent). The identifiers are D47's: `WORKOUT_FUTURE_SKEW_MAX_MS` and
`WORKOUT_STARTED_AT_PAST_MAX_MS`, both in milliseconds.

The numbers: 5 minutes future covers ordinary phone clock drift without NTP,
and nothing legitimate needs more, because the only v1 writer is "start a
session now" (Spec 06). 7 days past is generous for the same reason — there is
no backfill UI in v1 — and is deliberately not tighter, so a user who opens
the app after a week offline with a mis-set clock is not blocked outright.
Both bounds are guards against a device clock poisoning Spec 07's
`(user_id, started_at DESC)` history ordering, not product rules. Widening
either later is a pure relaxation and backward-compatible; narrowing is not,
which is the argument for erring generous now.

### 6.5 Finish

**`PATCH /v1/workouts/{id}` with `endedAt` set to a timestamp is the finish
transition.** There is no `POST /v1/workouts/{id}/finish`: finishing is
setting one column, the same `PATCH` that edits `title` and `notes` can do it
atomically with a last note, and an extra verb-shaped endpoint would need its
own schema and its own route for no additional behavior.

Rules, each independently assertable:

- `endedAt` must be **≥ `startedAt`**, else `422 validation-error`. Equality
  is allowed (a zero-length session is a user cancelling out, not an error).
- `endedAt` must be within the 5-minute future window of §6.4, else `422`.
- **A finished workout is immutable.** Any `PATCH` against a row that already
  has `ended_at` set is `409 workout-finished` (`WorkoutFinishedError`) — and
  the check runs on the **target's state, before the patch body is
  inspected**. That means `PATCH {}` against a finished workout is `409`, not
  a `200` no-op, even though `PATCH {}` against an in-progress workout is a
  `200` no-op (§5). The alternative — `{}` uniquely succeeding — makes the
  client model two rules instead of one ("a finished workout rejects writes"
  plus "except the empty one") to buy a way of re-fetching a row that
  `GET /v1/workouts/{id}` already provides.
- **`endedAt: null` never produces a `422`.** The schema accepts `null` (§5)
  precisely so that un-finishing reaches the handler and gets the answer it is
  owed: on a **finished** workout it is `409 workout-finished` (there is no
  un-finish in v1 — DESIGN §4.5 makes finished workouts immutable so Spec
  07's PR cache needs no invalidation path); on an **in-progress** workout,
  whose `ended_at` is already `NULL`, it is a **no-op** returning `200` with
  the row unchanged.
- **`workout-finished` is the single "the parent is immutable" signal.** It
  also guards `POST /v1/workouts/{id}/exercises`, `PATCH
  /v1/workout-exercises/{id}` and `DELETE /v1/workout-exercises/{id}` when the
  workout (respectively the parent workout) has `ended_at` set. A client has
  one slug to handle across all four writes.
- **`DELETE /v1/workouts/{id}` is the one exemption** and is allowed on a
  finished workout (DESIGN §4.5, §4.9: whole-workout hard delete is the
  sanctioned escape hatch from immutability). It hard-deletes and cascades to
  `workout_exercise`; a second `DELETE` of the same id is `404`, not a silent
  `204`, since after the first delete the row is indistinguishable from one
  that never existed.

**Extension seam 1 — Spec 05.1.** 05.1 adds the precondition that every
`working` set in the workout has at least one measure populated to *this*
transition, rejecting the finish when it does not hold; DESIGN §4.4 states
that rule as "enforced at finish", and `set_entry` does not exist until 05.1,
so there is nothing for 05.0 to check.

**Extension seam 2 — Spec 07.** 07 adds the personal-record recompute inside
this same transaction, and the matching recompute on `DELETE
/v1/workouts/{id}` (deleting a finished workout can retract a PR). DESIGN §4.5
says "PRs are written transactionally when a workout is finished", so reading
DESIGN literally, 05.0's finish is incomplete. **That is a deliberate split,
not an oversight:** `personal_record` is Spec 07's table in the M2 milestone,
and 05.0 cannot write rows to a table that does not exist. 07 both adds the
recompute here and backfills workouts finished before it ships. What 05.0 owes
07 is that the finish be a single transaction with one entry point in
`workoutRepository`, so 07 adds a call inside it rather than restructuring it.

### 6.6 Add exercise

`POST /v1/workouts/{id}/exercises`, in one transaction:

1. Load the workout via the repository with `actingUserId`; absent or another
   user's ⇒ `404 not-found` (§5). `ended_at` set ⇒ `409 workout-finished`.
2. Resolve `exerciseId` through
   `exerciseRepository.findVisibleById(actingUserId, exerciseId)`
   (`apps/api/src/repositories/exercise.ts`) — the method Spec 03.1 §3
   published for exactly this. A miss (absent, or another user's custom
   exercise) throws `NotFoundError` ⇒ `404`. `isActive === false` ⇒ `409
   exercise-retired` (`ExerciseRetiredError`, reused from 03.2): a retired
   exercise stays readable in history but is not something new sessions may
   add.
3. Copy `name` → `exercise_name_snapshot` and `modality` →
   `modality_snapshot` from the record returned in step 2, so the snapshot is
   a point-in-time copy of exactly the row the caller's `exerciseId` resolved
   to (§4, DESIGN §4.4). The resolve runs before the position transaction
   opens — `findVisibleById` is bound to the root Prisma client (Spec 03.1 §3)
   and takes no transaction client, and widening that signature is 03.1's
   change to make, not this spec's. A rename between the resolve and the
   insert is harmless: global rows are never renamed in place (DESIGN §4.2,
   append-only) and a custom row can only be renamed by the same user who is
   mid-request.
4. Insert at the resolved position (§6.7) and return `201` with a `Location`
   of `/v1/workout-exercises/{id}`.

Adding the **same** `exerciseId` twice to one workout is allowed and creates a
second row. Repeating an exercise later in a session is normal training, and
the two rows are distinguished by `position` and will carry different sets in
05.1. There is no idempotency key on this endpoint; a client that double-taps
gets two rows and deletes one.

### 6.7 Positions: append, reorder, and the deferred constraint

**A workout's exercise positions are always the dense sequence `0 … n-1`**,
zero-based (§5), with no gaps and no duplicates. Every mutation restores that
invariant before it commits, which is what makes the range checks below
statable and what lets `WorkoutDetail` order by `position` ascending and get
a stable list.

- **Add with `position` absent** appends at `n` (the current count).
- **Add with `position` present** requires `0 ≤ position ≤ n`; rows at
  `position` and above shift up by one. Out of range ⇒ `422
  validation-error`.
- **Reorder** (`PATCH /v1/workout-exercises/{id}` with `position`) requires
  `0 ≤ position ≤ n-1`; the row moves and the span between its old and new
  position is renumbered by one in the opposite direction. `position` equal to
  the row's current value is a `200` no-op. Out of range ⇒ `422`.
- **Delete** closes the gap: rows above the deleted one shift down by one.

**The range check is a repository/service-layer rule, not a schema rule**, for
two reasons. The schema cannot know `n` — it sees the body, not the workout.
And it is also what keeps `position` inside `smallint`: §5's DTOs bound
`position` below (`.min(0)`) but not above, so without this check a client
could send `2147483647` and get a Postgres `22003` numeric-overflow as a
`500`. Because `n` for a real workout is a few dozen at most, the `≤ n` /
`≤ n-1` check rejects everything above the `smallint` ceiling of 32767 long
before the column sees it. An implementer must therefore treat this check as
load-bearing, not cosmetic.

All four mutations run inside **one transaction**; the three that renumber
(add-at-position, reorder, delete) run it with the unique constraint deferred,
and a plain append is run the same way so there is one code path rather than
two:

```sql
BEGIN;
SET CONSTRAINTS workout_exercise_position_key DEFERRED;
-- renumber the affected span; intermediate states may collide, the commit cannot
COMMIT;
```

Deferral is needed even for the shifts that look safe. A set-based
`UPDATE workout_exercise SET position = position + 1 WHERE workout_id = $1 AND
position >= $2` updates rows in whatever order the plan produces; under the
constraint's `INITIALLY IMMEDIATE` default each row is checked as it is
written, so moving row `3` to `4` while row `4` is still at `4` fails, even
though the final state is unique. Deferring moves the check to `COMMIT`, where
only the final state is examined.

The alternatives were rejected:

- **No unique constraint at all.** Nothing then stops two rows sharing a
  position; the bug is silent, shows up as a list that reorders itself between
  reads, and is unrecoverable from the data. A constraint that costs one
  `SET CONSTRAINTS` on the reorder path is cheap insurance.
- **Fractional positions** (insert between `2.0` and `3.0` at `2.5`, never
  renumber). This is the right answer for collaborative documents with
  thousands of items and no write coordination. Here it buys nothing: a
  workout has a few dozen exercises, one writer, and a renumber is a
  single-digit-row `UPDATE`. In exchange it brings unbounded precision drift —
  repeated midpoint inserts run out of `float8` mantissa — plus a `numeric`
  column and a compaction job nobody would remember to write.

**The deferrable constraint must never be used as an `ON CONFLICT`
arbiter.** Postgres refuses to infer a conflict target from a deferrable
unique constraint, so `ON CONFLICT (workout_id, position)` fails outright —
which is fine, because nothing here wants upsert-on-position semantics. The
only `ON CONFLICT` in this spec is §6.1's, and it arbitrates on the
non-deferrable `workout_user_client_id_key`.

### 6.8 Concurrency

Two tabs on the same in-progress workout are **last-write-wins per row**, and
`updatedAt` is returned on every response so a client can detect that it lost
a race. This is DESIGN §4.4's existing posture restated, not re-litigated: v1
has one client, and real conflict resolution is a cost with no v1 payer.
DESIGN states it as "per set" because `set_entry` is the row it had in mind;
"per row" is the same rule generalised to the two tables this spec owns, and
§11 lists the one-line DESIGN edit that records the generalisation.

**The create path takes no lock.** Its two concurrency guarantees come from
the two unique indexes plus §6.2's re-reads (§4), and adding an advisory lock
on top would be redundant. When `ON CONFLICT … DO NOTHING`'s pre-check sees an
in-flight conflicting inserter it **waits** on it and re-checks once that
transaction completes, so "zero rows" implies the winner committed and the
re-read — a fresh `READ COMMITTED` statement whose snapshot is taken after
that — sees the committed row; when the pre-check was too early to see the
rival at all, step 4's re-read resolves the `23505` instead. Either way the
answer comes from data the database has already serialised for us, and
`workout_user_active_key` makes "at most one in-progress session" a constraint
the database enforces rather than a read the handler has to protect. A
per-user lock would add nothing to that and would put a serialization point on
the app's most-called write; it would also hold a pooled connection while
blocked, which is the connection-pool exposure 03.2's D20 left open for Spec
05.2. The correctness argument for the create path is §6.2's, and it needs no
lock to hold.

**Position mutations do need a lock**
(`pg_advisory_xact_lock(hashtext(workoutId))`, transaction-scoped), because
add, reorder and delete each read `n` — the current exercise count — and
then write rows whose positions depend on it. Two concurrent adds could
otherwise both compute the same `n` and collide on
`workout_exercise_position_key` at `COMMIT`, where the deferred check runs
and it is too late to renumber. The
lock is taken as its **own statement**, the first statement in the transaction
other than `SET CONSTRAINTS workout_exercise_position_key DEFERRED`,
following `insertWithCap` in
`apps/api/src/repositories/exercise.prisma.ts` (Spec 03.2 §6, D23). It is the
only advisory lock in this spec.

**That lock must be a separate statement from the read it protects.** Under
`READ COMMITTED` a statement's MVCC snapshot is fixed at that statement's
start, before the executor runs, so blocking mid-statement on
`pg_advisory_xact_lock` does not refresh the snapshot the rest of that
statement sees. A single statement that folds the lock into a CTE alongside
the `max(position)` read would take its snapshot before the lock was even
requested and read a stale `n` anyway — the exact trap Spec 03.2 §6 documents
at length and D23 forbids reintroducing. Both statements stay inside one
`prisma.$transaction`, so the lock is held across the read and the write and
released atomically with the commit.

`hashtext` returns 32 bits, so two distinct workouts can collide on a lock
key. The consequence is that two unrelated callers occasionally serialize
against each other for the duration of one short transaction — a latency blip,
never a correctness problem — and the key is not attacker-chosen: a caller
cannot address a workout that is not theirs (§5's 404 rule). The key space is
shared with `insertWithCap`'s `hashtext(actingUserId)` (03.2), so an alias can
also pair a custom-exercise create with an unrelated position mutation; the
consequence is identical.

### Wiring points

The concrete seams an implementer works against, so they are not rediscovered:

- **`apps/api/src/repositories/workout.ts`** (interface plus `WorkoutRecord`
  and `WorkoutExerciseRecord` types) and **`workout.prisma.ts`**
  (`createWorkoutRepository(prisma)` factory) — the same split as
  `exercise.ts` / `exercise.prisma.ts`. `actingUserId` is every method's first
  parameter. Ownership is checked with `assertOwned`
  (`apps/api/src/repositories/user.ts`), which takes a `{ userId }`-shaped row
  and throws `NotFoundError` — `workout` rows are that shape. A
  `workout_exercise` row has no `user_id`, so the repository loads it joined to
  its parent workout and applies `assertOwned` to the parent. Pure, DB-free
  helpers (position arithmetic, the skew bound) get their own file, as
  `exercise-writes.ts` did, so they are unit-testable without a database.
- **`apps/api/src/routes/workouts.ts`** exporting
  `registerWorkoutRoutes(app, deps)`, registered at **four** points:
  `V1RouteDeps` and `registerV1Routes` in `apps/api/src/routes/v1.ts`,
  `BuildAppDeps` in `apps/api/src/app.ts`, and the composition root in
  `apps/api/src/server.ts`. Missing the last one yields an app that passes
  every unit test and serves 404s in staging.
- **Every route needs a declared response schema.**
  `assertRouteHasResponseSchema` (`apps/api/src/app.ts`, wired into the
  `onRoute` hook) throws at assembly time for any route without one —
  **including the two `204` deletes**. Copy whatever the existing `DELETE` in
  `apps/api/src/routes/exercises.ts` declares rather than inventing a second
  convention.
- **The `tzOffsetMinutes` fallback** (§6.3) reads `user.timezone` through the
  existing Spec 01 `userRepository` — the row is already loaded for
  `request.user` — and `user.timezone` is `NOT NULL DEFAULT 'UTC'`
  (`apps/api/prisma/schema.prisma`), so the fallback always resolves and needs
  no second default.
- **`apps/api/src/errors/app-error.ts`** gains `WorkoutFinishedError` and
  `WorkoutInProgressExistsError` exactly as listed in §5. `NotFoundError`,
  `ValidationError` and `ExerciseRetiredError` are reused.
- **Prisma models `Workout` and `WorkoutExercise`** with `@@map` / `@map`
  snake_case mappings, plus the back-reference fields both relations oblige
  (`User.workouts`, `Exercise.workoutExercises`), or `prisma validate` fails
  in CI — the trap 03.1 §4 recorded for `User.customExercises` (§4).
- **Tests.** `apps/api/test/helpers/fakes.ts` gains `makeWorkoutRecord` and
  `FakeWorkoutRepository`; `buildTestApp` gains a `workoutRepository` option
  defaulting to the fake. Unit tests authenticate with a plain
  `Bearer test-token` against `fakeVerifier` — **there is no JWT minting in
  unit tests**; the dev-idp on `:9999` is for manual and integration use.
- **Free-text bounds stay well inside `BODY_LIMIT_BYTES`**
  (`apps/api/src/app.ts`, 64 KiB). `WORKOUT_TITLE_MAX` (120) plus
  `WORKOUT_NOTES_MAX` (4000) is at most ~16 KB even at 4 bytes per character,
  so an over-long field is always `422 validation-error` from the schema and
  never `413 payload-too-large` from the body parser — which matters because
  `413` carries no field path and a user would have no idea which field to
  shorten.

---

## 7. Security & privacy

- **First table whose rows are entirely user-authored free text.**
  `workout.title`, `workout.notes` and `workout_exercise.notes` are arbitrary
  text from the caller, with no curated row behind any of them. Spec 03.2
  raised the same stakes for `exercise.name`; here nothing in the row is
  trusted. The storage contract is the 03.1 §7 `CatalogName` argument restated
  for free text: **bound length and control characters at ingress, store
  verbatim, output-encode at render time** (Spec 06). All three fields go
  through a shared `@sin/core` refine (§5) rather than per-route checks, so
  the create, update and add-exercise paths cannot drift apart.
- **Why those bounds.** `title` is `1..120` characters, the same ceiling as
  `CatalogName`: it is a single line shown in a list row, and 120 is longer
  than any real session name. `notes` is `1..4000`: several paragraphs of
  training prose, an order of magnitude more than anyone types on a phone in
  a gym, and small enough that a hostile client cannot use a notes field as
  free storage. The minimum of 1 means an empty string is `422`; a client that
  wants "no title" sends `null` or omits the key, so the column has one absent
  representation (`NULL`), not two. Zod counts length in UTF-16 code units, so
  an emoji costs two; that is stricter than the `text` column needs and is the
  safe direction for a bound.
- **Why reject control characters, and which.** `title` rejects every C0
  control character and DEL (`noControlChars`, reused from `dto/exercise.ts`).
  `notes` uses a second predicate that permits TAB, LF and CR, because notes
  are multi-line, and rejects the rest (§5). Control characters have no
  legitimate use in either field and do real damage downstream: NUL is
  refused by Postgres `text` outright (a `500` if it got that far), and the
  rest corrupt terminal log viewers, CSV export (Spec 11) and any
  line-oriented tool. Rejecting at ingress with `422` is cheaper and safer
  than sanitising on every read. Note this is a **format** check, not an
  HTML filter: `<script>` is a legal title.
- **Stored verbatim; encoding is the renderer's job.** The API does not
  HTML-escape, strip or normalise either field, so a stored value round-trips
  byte-for-byte. Escaping at write time would be wrong for every non-HTML
  consumer (the JSON API, native clients, export) and would double-encode in
  Spec 06's React render. React's default text rendering already encodes, and
  Spec 06 owns the rule that these fields are only ever rendered as text,
  never as HTML.
- **The bounds sit well inside `BODY_LIMIT_BYTES`.** The app-wide limit is 64
  KiB (`apps/api/src/app.ts`). A request that combines the largest title and
  notes is ~16 KB at 4 bytes per UTF-8 character; the adversarial encoding is
  worse, because a client may send every character as a JSON `\uXXXX` escape
  (6 bytes per code unit), so 4000 notes characters inflate to roughly 24 KB
  on the wire. That is still under half the limit. An over-long field is
  therefore always `422 validation-error` naming the field, never `413
  payload-too-large`, which carries no field path (§6, Wiring points). A body
  that is genuinely over 64 KiB is `413`, before the schema runs, and needs no
  handling here.
- **Every query goes through the repository (DESIGN R8), `actingUserId`
  first.** R8 mandates the repository layer itself — it is the seam the social
  phase's authorization layer will attach to — and the 03.1 / 03.2 convention
  implements it by making `actingUserId` every method's first parameter, so
  the rule is structural rather than a matter of reviewer vigilance: every
  method on `workoutRepository` takes `actingUserId` first, and no
  handler queries `workout` or `workout_exercise` by id alone (§6). Ownership
  of a `workout` row is `assertOwned`; a `workout_exercise` row has no
  `user_id`, so the repository resolves it through its parent workout and
  applies the same check (§5, §6 Wiring points). There is deliberately no
  "get workout by id, trust the caller" method for a handler to reach for. The
  cross-user integration tests (§10) assert this on every route that takes an
  id.
- **404, never 403, for another user's row.** A workout or workout exercise
  belonging to someone else is indistinguishable on the wire from one that
  does not exist (§5), so a caller cannot probe for other users' ids. The same
  holds for `exerciseId` in the add-exercise body: another user's custom
  exercise is `404`, resolved by `exerciseRepository.findVisibleById`, so this
  endpoint cannot be used to test whether someone else's exercise exists. A
  malformed `{id}` is also `404`, so "wrong shape" and "not yours" cannot be
  told apart either. The `409 workout-in-progress-exists` body carries no
  workout id (§6.2), so even that error hands the caller nothing beyond what
  `GET /v1/workouts/active` (their own row only) already returns.
- **No client-settable ownership or provenance.** Request bodies are
  `z.strictObject` (§5), so `userId`, `localDate`, `source`, `id` and the
  snapshot fields are `422`, not silently dropped: a client cannot write a row
  into another user's account, forge a `local_date`, or supply its own
  `exercise_name_snapshot`.
- **Structured logs carry ids and durations only, never free text.** The
  lines in §9 record `workout_id`, `workout_exercise_id`, `exercise_id`,
  `user_id` and durations. `title` and `notes` are arbitrary user text with no
  business being duplicated into logs, the same rule as 03.2 §7 for `name`.
  The RFC 9457 `detail` for `422` names the failing field and rule, never
  echoes the submitted value.
- **No write throttle and no per-user quota in this spec.** 05.0 ships
  neither, and this is a stated gap, not an oversight. The repository has no
  rate-limit plugin or dependency today (`@fastify/rate-limit` and its
  equivalents are absent from `apps/api/package.json`), so **Spec 05.2 is
  greenfield**: it introduces the mechanism and the quotas, it does not extend
  something that exists. Until it lands, an authenticated user can create,
  delete and re-create workouts, and add exercises to one, as fast as the
  connection allows. Two of 03.2's open decisions therefore **stay open**:
  **D20** (advisory-lock connection-pool blast radius) and **D21** (unbounded
  storage growth via create/delete cycling). The per-workout position lock
  (§6.8) is a correctness lock scoped to one workout, held for one short
  transaction; it does **not** close D20, and this spec makes no claim that
  it does. The one-in-progress rule bounds *concurrent* open sessions per
  user to one, which limits nothing about *sequential* volume, so it does not
  address D21 either. The only volume bound in this spec is per-request:
  bounded free text and the 64 KiB body limit.
- **No new PII.** A workout's calendar fields, free text and exercise choices
  are the user's own data and are scoped to them by the rules above; the row
  carries no name, email or location. `tz_offset_minutes` is coarser than the
  `user.timezone` Spec 02 already stores. Whole-account export and purge are
  Spec 11's, and the `ON DELETE CASCADE` chain in §4 is what lets that purge
  reach these rows.

---

## 8. Config & secrets

**No new environment variables and no new secrets.** Everything this spec
tunes is a code constant in `@sin/core`, deliberately not env-configurable:

- The two `startedAt` clock-skew bounds of §6.4 (5 minutes into the future,
  7 days into the past, the former also applied to `endedAt` on finish),
  defined in `packages/core/src/dto/workout.ts` alongside `WORKOUT_TITLE_MAX`
  and `WORKOUT_NOTES_MAX`. §6.4 fixes the values and where they live and D47
  fixes the identifiers — `WORKOUT_FUTURE_SKEW_MAX_MS` (300 000) and
  `WORKOUT_STARTED_AT_PAST_MAX_MS` (604 800 000), both in milliseconds.
- `WORKOUT_TITLE_MAX` (120) and `WORKOUT_NOTES_MAX` (4000), which §7 argues.
- `WORKOUT_SOURCE_VALUES`, which renders the `source` `CHECK` (§4).

This follows the `MAX_CUSTOM_EXERCISES_PER_USER` precedent (03.2 §8): changing
any of these is a **code-reviewed decision, not an ops knob**. The skew bounds
guard the `(user_id, started_at DESC)` history ordering that Spec 07 reads;
the text bounds are tied to the body-limit argument in §7; the `source` list
is tied to a database `CHECK` that only a migration can change. Making any of
them an env var would let a staging value silently differ from production and
would make the behavior a spec cannot state as a number. The bounds are
shared with the client through `@sin/core`, so Spec 06 validates against the
same values the server enforces.

`DATABASE_URL`, the Auth0 settings and `BODY_LIMIT_BYTES` are unchanged and
owned by Specs 01 and 03.0. Nothing here needs a Render or CI setting.

---

## 9. Observability

The Spec 01 request log (`request_id`, `user_id`, status, duration) already
covers all eight routes. This spec adds four structured log lines, one per
business event, emitted by the handler after the write commits:

| Line | Emitted when | Fields |
|---|---|---|
| `workout_started` | `POST /v1/workouts` creates a row (`201`) | `workout_id`, `user_id` |
| `workout_finished` | `PATCH /v1/workouts/{id}` sets `endedAt` on an in-progress workout | `workout_id`, `user_id`, `duration_seconds` (`endedAt` minus `startedAt`), `exercise_count` |
| `workout_deleted` | `DELETE /v1/workouts/{id}` succeeds | `workout_id`, `user_id`, `was_finished`, `exercise_count` |
| `workout_exercise_added` | `POST /v1/workouts/{id}/exercises` succeeds | `workout_id`, `workout_exercise_id`, `exercise_id`, `user_id`, `position` |

- **Ids, counts and durations only, never `title` or `notes`** (§7). Field
  names are snake_case, matching the 03.2 §9 lines.
- **`exercise_count` comes from the write's own transaction** — a `count` in
  the finish transaction, and the number of cascaded child rows the delete
  reports — not from a second round trip after the response.
- **`workout_started` is emitted on the fresh-create path only.** The `200`
  idempotent replay (§6.1) does not log it, so a flaky-connection retry does
  not inflate the started count. A client's retry storm is visible in the
  request log, where the `200` / `201` split already distinguishes the two.
- **`workout_finished` fires on the transition, not on every `PATCH`.** A
  `PATCH` that only edits `title` or `notes`, or a `PATCH {}` no-op, logs
  nothing beyond the request line.
- **These lines are the business counters.** DESIGN §8.1 lists "business
  counters (workouts started/finished, sets logged)" among its metrics; the
  first two lines are their source for workouts, and 05.1 adds the sets-logged
  counterpart. DESIGN §1.3 has no metric literally named "workouts
  started/finished", but its "workouts with all sets logged during the
  session" target is a ratio over finished workouts, so `workout_finished`
  supplies its denominator (the numerator needs 05.1's set data). There
  is no metrics backend yet (Spec 13), so today these are log fields that can
  be counted from Render's log stream; Spec 13 lifts them into real counters
  without changing the lines.
- **BL-5 applies here and is not fixed here.** Routine `404` / `409` / `422`
  outcomes on these endpoints (`workout-finished`,
  `workout-in-progress-exists`, `exercise-retired`, a clock-skew `422`) have no
  dedicated log line, so they fall to the generic `error`-level handler until
  BL-5 is resolved. On these routes that is expected traffic: a resumed
  session that hits `workout-in-progress-exists` is the designed recovery path
  (§5), not an incident. Anyone alerting on `error` rate should read it with
  that in mind. This spec does not change the handler.
- No traces or metrics backend yet (Spec 13); DESIGN §8.1 asks for the finish
  transaction to be traced specifically, which is Spec 13's to wire.

---

## 10. Testing

Method as Spec 01 §10 and 03.1 §10: test-first per criterion, Vitest, HTTP via
`fastify.inject`, integration via **Testcontainers Postgres against the real
Prisma migrations** (`startIntegrationDb` / `applyMigrationFile` in
`apps/api/test/integration/helpers.ts`). Order of attack: migration →
`@sin/core` ids / DTOs / `time.ts` → `workoutRepository` → routes → the OpenAPI
assertion. Route-level unit tests run against `FakeWorkoutRepository` through
`buildTestApp`, authenticating with a plain `Bearer test-token` against
`fakeVerifier` — there is no JWT minting in unit tests (§6, Wiring points).

The new integration tests follow the existing **`RUN_INTEGRATION` gate**
(`shouldRunIntegration()`), and their container-startup `beforeAll` is
**guarded on the same condition** — not just the `describe` — because
`describe.skipIf` still runs `beforeAll` (CLAUDE.md). A plain
`pnpm --filter @sin/api run test:unit` must not boot Postgres.

| Criterion | Verified by |
|---|---|
| AC1 | Integration (this spec's migration test): migrate a fresh container and one stopped at the previous migration; assert columns / types / nullability, the three `ON DELETE CASCADE` FKs, both `CHECK`s and all five named indexes via `information_schema`, `pg_indexes` and `pg_constraint` (asserting `condeferrable` on `workout_exercise_position_key` and `indpred` on `workout_user_active_key`). The no-drift assertion lives in that **same integration test**, created here on 03.3 AC1 / D30's pattern: `spawnSync("pnpm", ["exec","prisma","migrate","diff","--from-url",db.url,"--to-schema-datamodel","prisma/schema.prisma","--exit-code"])` expecting "No difference detected" — `spawnSync`, not `execFileSync` in a `try`, so a CLI that fails to run at all fails the test rather than looking like no diff (`apps/api/test/integration/exercise-change-xid-migration.integration.test.ts:128`). The only CI item is the existing `prisma validate`; no new CI step |
| AC2 | Integration (primary): raw insert of `source='bogus'` and of `modality_snapshot='bogus'` → rejected; each `WORKOUT_SOURCE_VALUES` / `MODALITY_VALUES` member → accepted. Unit (drift guard): read `0005_create_workout_session/migration.sql` and assert it **contains** each clause rendered from the frozen constant — string containment for **both** lists, *not* a `pg_get_constraintdef()` parse (03.1 AC2) |
| AC3 | **A demanding row.** Integration: `POST` → `201` + `Location`; replay → `200`, no `Location` header, stored row byte-identical though the replayed body differs; one row in the table. Then the concurrency case: **~10 parallel requests** with one identical `clientGeneratedId` ⇒ exactly one row, exactly one `201`, the rest `200`. Ten, not two: 03.2's **D23** is the cautionary precedent — a weak two-caller race test passes against a broken implementation on scheduling luck alone. Assert **no response is `409`**: under D50 that holds regardless of which unique index each loser resolved its conflict on, so the row is a deterministic assertion rather than a flaky one |
| AC4 | **A demanding row.** Integration: create, then a second create with a different `clientGeneratedId` → `409 workout-in-progress-exists`, asserting the body has the expected problem+json members **and no workout id**; `GET /v1/workouts/active` → `200` with that row, and `404` for a user with none. Then a **concurrent burst of creates** (distinct `clientGeneratedId`s) for a user with no active session ⇒ exactly one `201` and the rest `409`, one row with `ended_at IS NULL` |
| AC5 | Integration: create, then replay the *same* `clientGeneratedId` while that workout is still in progress → `200`, never `409`. Unit on the **real** repository — `createWorkoutRepository(stubPrisma)`, because the `23505` mapping and the retry live in `workout.prisma.ts`, which `FakeWorkoutRepository` replaces wholesale (the fake stays a route-level test double): (a) the stub's insert rejects with a Prisma error carrying SQLSTATE `23505` and a chosen constraint name, shaped as the raw-query path is expected to surface it — a `$queryRaw` insert should yield `PrismaClientKnownRequestError` with `code: "P2010"` and the driver's SQLSTATE and message under `meta` (the constraint name is **parsed out of `meta.message`**, `duplicate key value violates unique constraint "…"`), as opposed to the typed-client `P2002` + `meta.target`. The integration suite must confirm that shape against one real Testcontainers failure before the stub is written to it, because no repository parses Prisma error codes today and this test pins the idiom 05.0 introduces. The `23505` cases, per D50: constraint `workout_user_active_key` **plus a re-read that finds a row** → `200` with that stored row; the same `23505` with an **empty** re-read → `409 workout-in-progress-exists`; any other constraint name → `500` with no re-read attempted. (b) the stub returns zero rows then an empty re-read → the insert is retried exactly once and, when the retry returns a row, the response is `201`; zero rows plus an empty re-read a **second** time → an internal error (`500`), asserting the retry does not loop. All branches are how `workout.prisma.ts` reaches its coverage target |
| AC6 | Unit (`@sin/core`): `localDateFor` across an offset that moves the date in each direction. Integration: `POST` with an explicit `tzOffsetMinutes` → stored `local_date` / `tz_offset_minutes`; `POST` with the key absent for a user whose `timezone` is set → the offset **at that instant**, asserted on both sides of a DST transition; then, in order, `PATCH` the in-progress workout's `title` and `notes`, **then** finish it, asserting `local_date` is unchanged after each step (the `PATCH` must precede the finish — a `PATCH` on a finished workout is `409 workout-finished`, AC8); `localDate` in a `POST` body → `422`, and `localDate` or `tzOffsetMinutes` in a `PATCH` body → `422` |
| AC7 | Unit on the pure skew helper (§6, Wiring points) with a fixed injected clock: just inside and just outside each of §6.4's +5 min / −7 days bounds. `inject` tests for the `422` on `POST` `startedAt` and on the finish `endedAt` |
| AC8 | `inject`: finish an in-progress workout → `200` with `endedAt`; `endedAt < startedAt` → `422`; `endedAt === startedAt` → `200`; on a finished workout `PATCH { title }`, `PATCH {}` and `PATCH { endedAt: null }` each → `409 workout-finished`; on an in-progress workout `PATCH {}` and `PATCH { endedAt: null }` each → `200` with the row unchanged |
| AC9 | `inject` against a finished workout: `POST /v1/workouts/{id}/exercises`, `PATCH` and `DELETE /v1/workout-exercises/{id}` → `409 workout-finished` (same slug asserted on all three); `DELETE /v1/workouts/{id}` → `204` on a finished workout and on an in-progress one; integration assertion that the child rows are gone; repeat `DELETE` → `404` |
| AC10 | Integration: add a global exercise → `201` + `Location`, snapshots equal to the row `findVisibleById` resolved; then rename and soft-delete that exercise and re-read the workout → snapshots unchanged. `inject`: an absent `exerciseId` and another user's custom `exerciseId` → `404`; an `isActive=false` exercise → `409 exercise-retired`; the same `exerciseId` added twice → two rows at distinct positions |
| AC11 | Integration: a table-driven sequence of add-append, add-at-position, reorder both directions and delete, asserting the full position list is `0 … n-1` after each step and that `GET /v1/workouts/{id}` returns them in that order. `inject`: `position = n+1` on add, `position = n` on reorder, and `position = 40000` (above the `smallint` ceiling) → `422` in every case, asserting no `500` and no Postgres `22003` reaches the client. Unit tests on the pure position-arithmetic helper |
| AC12 | **A demanding row.** Integration, through the repository's reorder path: a **two-row swap** whose intermediate state puts two rows on the same position — an operation that violates `workout_exercise_position_key` unless the transaction has run `SET CONSTRAINTS … DEFERRED` — commits, and the final positions are dense `0 … n-1`. Then N concurrent `POST /v1/workouts/{id}/exercises` against one workout ⇒ all `201`, no `500`, no `23505` at `COMMIT`, dense final sequence |
| AC13 | Unit on `createWorkoutRepository(stubPrisma)` (or Prisma's `query` event on the integration client): capture the statements the add / reorder / delete transaction issues and assert the **first statement other than `SET CONSTRAINTS workout_exercise_position_key DEFERRED`** matches `pg_advisory_xact_lock`, and that the `count` / `max(position)` read is a **later, separate** statement — a call-order assertion, so a CTE that folds the two together fails it |
| AC14 | The same statement capture applied to the create path behind `POST /v1/workouts`: assert no captured statement contains `pg_advisory_xact_lock` |
| AC15 | Integration cross-user suite over **all six id-taking routes**: user B's workout / workout exercise, and a syntactically malformed id, each → `404` whose `type`, `title`, `status` and `detail` are identical (only `instance` differs, since it echoes the request URI), asserted field by field; no route ever answers `403`. Plus the add-exercise body case: user B's custom `exerciseId` → `404` |
| AC16 | Unit (`@sin/core`): each `strictObject` rejects an unknown key (`userId`, `id`, `localDate`, `source`, a snapshot field); `title` at 120 and 121 characters; `notes` at 4000 and 4001; `""` → `422`; `null` and omission accepted; a NUL, a TAB in `title`, and a TAB / LF / CR in `notes`. `inject`: a body whose `notes` is `WORKOUT_NOTES_MAX + 1` characters, sent as `\uXXXX` escapes so the wire body is ~24 KB (still under `BODY_LIMIT_BYTES`), → `422` naming the field, with **`413` explicitly asserted absent**; and separately a body at exactly 120 / 4000 characters → `201`; and a round-trip asserting `<script>alert(1)</script>` is stored and returned verbatim |
| AC17 | **A demanding row.** Integration: build a user with a workout containing one **global** exercise and one of that user's **own custom** exercises, plus a second user with their own workout. `DELETE FROM "user" WHERE id = $1` in a single statement ⇒ succeeds with no FK error, the user's `workout` and all their `workout_exercise` rows are gone down **both** paths (`user → workout → workout_exercise` and `user → exercise → workout_exercise`), the global `exercise` row survives, and the second user's rows are untouched. This is the test that proves §4's FK direction and discharges 03.1 §3 / 03.2 §3's purge-ordering obligation |
| AC18 | Unit, using the repo's existing log-capture pattern — `buildTestApp({ logger: pino({ level: "debug" }, { write: … }) })`, as `apps/api/test/unit/routes-exercises.test.ts` does for `sync-token-expired`: assert `workout_started` on the `201` and **absent** on the `200` replay; `workout_finished` present on the finish `PATCH` and absent on a `title`-only `PATCH` and on `PATCH {}`; `workout_deleted` and `workout_exercise_added` present with their §9 fields; and that no captured line, over a request carrying a distinctive title and notes, contains either string |
| AC19 | `@sin/core` Vitest: `parseWorkoutId` / `parseWorkoutExerciseId` accept a UUID and throw on malformed input, with `expectTypeOf` negative assertions under `--typecheck` that `string` ⊄ `WorkoutId` and `WorkoutId` ⊄ `WorkoutExerciseId`; `Object.isFrozen(WORKOUT_SOURCE_VALUES)`; parse / reject cases for the seven DTOs; `localDateFor` and `offsetMinutesForZone` unit cases (DST boundary, negative offset, bare `GMT` → `0`). Existing gates: `check-exports.mjs`, `pnpm run core:purity`, `test/deps.test.ts` |
| AC20 | `inject` no-token and invalid-token `401` on all eight routes; OpenAPI emit assertion that `paths` contains all eight with camelCase component schemas and that the two `204` deletes carry a declared response schema (`assertRouteHasResponseSchema` would otherwise throw at assembly, which a `buildApp` smoke test also covers); CI OpenAPI drift check |
| AC21 | Review of the DESIGN.md / `docs/specs/README.md` diff in the implementation PR, against the list in §11 |

Four of those rows are deliberately more demanding than a happy-path check,
and are called out here rather than left to a test author's taste: **AC3**
(≈10 parallel identical `clientGeneratedId`s, per 03.2 D23), **AC4** (a
concurrent burst of creates against the partial unique index), **AC12** (a
two-row swap that needs the deferred constraint, plus concurrent adds under the
advisory lock) and **AC17** (a purge that proves the FK direction down both
cascade paths). Each of the four is the cheapest test that would actually fail
against the plausible broken implementation; a weaker version of any of them
passes against code that is wrong.

**A review-time note, not an assertion.** "AC12's swap would fail if
`SET CONSTRAINTS workout_exercise_position_key DEFERRED` were removed" is a
mutation-testing claim: no assertion in the suite can state it, because the
suite only ever runs the shipped code. It is recorded here so a reviewer can
check it by hand once — delete the `SET CONSTRAINTS` statement locally and
confirm AC12's swap turns red — and so nobody tries to encode it as a test.
The same applies to "a CTE folding the lock into the count read would be
wrong": AC13's assertion is the positive, checkable form of it (first
statement is the lock, the read is a later one).

**Coverage.** `apps/api/src/repositories/workout.ts` /
`workout.prisma.ts` and the pure position/skew helpers sit at or near 100% line
coverage, for the reason 03.1 §10 and 03.2 §10 both gave: a silent bug here —
admitting a second active session, leaking another user's workout through a
write path, or losing a set's parent by mis-renumbering a position — is the
class of bug this repo treats as high-severity, and none of it is visible from
the outside until a user's history is already wrong. The Spec 01 ≥90% gate on
`apps/api/src/plugins/auth` and `apps/api/src/repositories/user` is unchanged.

**CI.** No new job: the new integration tests join the existing
`RUN_INTEGRATION` job, and the OpenAPI drift-check step already exists (Spec
03.0).

---

## 11. Deployment & rollback

`0005_create_workout_session` is additive and expand-only: it creates two new
tables and their indexes and constraints, and touches no existing table, column
or row. The `User.workouts` and `Exercise.workoutExercises` back-references
(§4) are Prisma-client-only fields with no column behind them. It is therefore
safe to run ahead of the code that uses it: the currently deployed image does
not know the tables exist. No seed step is touched by this spec.

**Release ordering** (extends Spec 01 §11 and 03.2 / 03.3 §11):

1. `prisma migrate deploy` applies `0005_create_workout_session`. For Neon, run
   it against the **direct** (non-pooler) host (CLAUDE.md). **Run it before
   merging:** a merge to `main` auto-deploys the code (Render `autoDeploy`)
   while `migrate deploy` is a manual step, and the new routes query the two
   tables, so code that went live first would answer `500` on every workout
   route until the migration landed. Nothing calls them yet, so that would be
   harmless, but ordering it correctly costs nothing (the same reasoning as
   03.3 §11).
2. New code goes live. The eight endpoints start serving.
3. `/openapi.json` reflects them on the next emit; the CI drift check catches a
   mismatch before merge (AC20).

**No feature flag.** "Off" is simply the absence of callers until Spec 06
exists. `apps/api/scripts/smoke.ts` is not extended: every new route is
authenticated, and AC20's `401` assertions and the OpenAPI drift check already
cover the published surface before the code ships.

**Rollback:** revert the code commit. Any `workout` / `workout_exercise` rows
already written stay valid, inert data — nothing else reads them until Specs
05.1 and 07 exist, and both would tolerate rows written by any 05.0 code
version. The tables are left in place; production undo is a forward migration
if one is ever wanted, never a down script (Spec 01 §11). The down migration
(drops `workout_exercise`, then `workout`) stays dev/test-only, as in 03.1's
and 03.3's convention. Once Spec 05.1 ships its `set_entry` FK to
`workout_exercise`, reverting 05.0's *code* alone is no longer independent of
05.1 — a constraint on that later spec's rollback section, not something to
solve here.

### DESIGN.md / doc edits

> Listed for traceability, matching 03.2's convention (§11 there) — applied
> when this spec is implemented, not now (draft = no repo changes beyond this
> spec file and the README). The implementation PR verifies they are present
> (AC21).

- **DESIGN §4.4** — reconcile the `workout` / `workout_exercise` column lists
  with what ships: drop `routine_id` from `workout` and `superset_group` from
  `workout_exercise`, with a one-line note that Spec 09 adds both additively
  (D36). The same section should also gain the two invariants it does not state
  today, since DESIGN is the source of truth for the data model: **at most one
  in-progress workout per user** (partial unique index on `user_id WHERE
  ended_at IS NULL`, D38) and **a workout's exercise `position`s are the dense
  zero-based sequence `0 … n-1`**, unique per workout (D41). Neither appears
  in DESIGN today, so the omission is a gap this spec creates, not one it
  inherits. The same section's concurrency paragraph should generalise
  "last-write-wins **per set**" to "per row (per set once `set_entry`
  exists)", so §6.8's restatement is recorded rather than a silent widening.
- **DESIGN §4.5** — one line recording that the personal-record write at
  finish is **Spec 07's**, not 05.x's, and that Spec 07 also owns the recompute
  on `DELETE /workouts/{id}`, so the paragraph "PRs are written transactionally
  when a workout is finished" is not read as describing behavior 05.0 ships
  (D35). The M1 finish (05.0) sets `ended_at` only; the M2 finish (07) adds
  the PR write.
- **DESIGN §4.9** — confirm, do not pre-edit. The `workout` /
  `workout_exercise` row's "hard delete (cascade)" matches §6.5's
  `DELETE /v1/workouts/{id}` exactly (AC9). One reference in the same table is
  now discharged rather than pending: the custom-`exercise` row says the purge
  job "must delete `workout_exercise` rows that reference it **before / with**
  the user's custom `exercise` rows — Spec 05 defines that FK with purge
  ordering in mind". Spec 05.0 defines it as `ON DELETE CASCADE` (D37), so the
  database supplies that ordering and the purge job carries no such
  obligation; the implementation PR should point the cell at Spec 05.0 (not
  the retired unsplit "Spec 05") and say the ordering is now the FK's job. That
  is a wording reconciliation, not a change to the matrix's rules.
- **DESIGN §6** (API Design) — two reconciliations. The "Time" bullet should
  gain the clock-skew rule (D47): a client-supplied `started_at` more than 5
  minutes ahead of, or more than 7 days behind, the server clock is rejected
  (`422`), and `ended_at` on finish is bound by the same 5-minute future
  window. The "Representative endpoints" block should be brought into line with
  the eight shipped routes: `POST /workouts` drops `routine_id?` (D36) and
  returns `200` on replay; add `GET /workouts/active` (§5); `DELETE
  /workouts/{id}`'s comment ("triggers PR recompute") becomes "PR recompute is
  Spec 07's" (D35); `POST /workouts/{id}/exercises` takes an optional
  `position`; add `PATCH` and `DELETE /workout-exercises/{id}`. The block's
  snake_case body keys are a pre-existing shorthand, contradicted by the
  "JSON casing" bullet directly above it; this spec does not depend on fixing
  them and leaves them alone.
- **DESIGN §1.3, §8.1** — **no change.** §9 of this spec cites §1.3's
  "workouts with all sets logged during the session" target and §8.1's
  "business counters" line accurately; the metric it says §1.3 does *not*
  name is a clarification in this spec's own prose, not a DESIGN defect.
  DESIGN §9 (rollout plan) is deliberately left alone: its M1 line already
  reads "start/empty workout; log sets; finish", which 05.0 + 05.1 + 06
  together satisfy.
- **`docs/specs/README.md`** — the roadmap rows: replace the single `05` row
  with `05.0` / `05.1` / `05.2`; repoint `06`'s dependencies at `05.0` and
  `05.1` and `07`'s at `05.1`; move the rate-limiting / write-quota ownership
  line to Spec 05.2; repoint the `packages/core` domain-math note at 05.1 / 07;
  set the `03.2` row's status to `Implemented` (it still reads `Draft`). This
  is applied as a separate roadmap-doc task in the same drafting effort, not
  by the implementation PR.

---

## 12. Decisions & open questions

Decision numbers are global across the specs (03.1 has D1–D13, 03.2 D14–D23,
03.3 D24–D33), so 05.0 continues at **D34**. Nothing in this section amends an
earlier decision; D20 and D21 are carried forward unresolved, under "Open".

### Resolved

- ✅ **D34 — split Spec 05 into 05.0 / 05.1 / 05.2.** 05.0 is the session
  lifecycle (this spec); 05.1 is set logging (`set_entry`, per-modality
  validation, the finish integrity rule); 05.2 is rate limiting and per-user
  write quotas. The roadmap's single Spec 05 bundled at least three work
  sessions of change — two tables plus a dozen-odd endpoints, a modality-shaped
  validation layer, and a greenfield throttle mechanism — against the rule that
  a spec fits one session. The seams are also real dependency edges: 05.1's
  `set_entry` FKs to `workout_exercise`, which must exist first; and 05.2 is
  independent of the data model and best designed after the write paths it
  throttles exist. **API-first** holds (Spec 06 is the UI). **Rejected:** *one
  spec* — cannot be reviewed or finished in a session, and would stall the
  simple lifecycle behind the hard measure-validation work. *A two-way split
  (05.0 lifecycle + sets, 05.1 rate limiting)* — sets are still a full session
  by themselves (per-modality measure rules, `PUT` idempotency, the finish
  integrity rule) and would leave 05.0 too large. *Splitting the lifecycle
  further (create/finish vs exercises)* — the two halves share the tables, the
  immutability rule and the position lock, and would each need most of the
  same review. **Consequence:** `06` depends on 05.0 and 05.1; `07` on 05.1;
  §11's README edits carry this.
- ✅ **D35 — personal-record writes at finish stay in Spec 07, not 05.x.**
  DESIGN §4.5 says PRs are "written transactionally when a workout is
  finished", which read literally makes 05.0's finish incomplete. It is a
  deliberate split: `personal_record` is Spec 07's table (M2) and 05.0 cannot
  write rows to a table that does not exist. 05.0's finish sets `ended_at` and
  nothing else; **07** adds the PR recompute inside the same transaction, the
  matching recompute on `DELETE /v1/workouts/{id}`, and a backfill for
  workouts finished before it ships. What 05.0 owes 07 is the seam: finish is
  one transaction with one entry point in `workoutRepository` (§6.5, Extension
  seam 2), so 07 adds a call rather than restructuring. **Rejected:** *create
  `personal_record` in 05.x and write it at finish* — drags M2's table, its
  three record-type rules and their Risk R4 test burden into the session
  lifecycle, and would put an unreviewed slice of PR math on the hot path
  before its own spec. *A stub `personal_record` table with no writer* —
  ships schema nobody exercises, to be reworked when 07's real column set is
  settled. **Consequence:** DESIGN §4.5 gets a one-line note (§11) so it is
  not read as shipped behavior.
- ✅ **D36 — `routine_id` and `superset_group` are omitted until Spec 09.**
  DESIGN §4.4 lists both, but nothing in M1 writes or reads either.
  `superset_group` is added by Spec 09 with `routine_item.superset_group`;
  `routine_id` waits because the `routine` table does not exist until 09. Both
  additions are one additive `ALTER TABLE … ADD COLUMN` there (the `routine_id`
  one carries `REFERENCES routine(id) ON DELETE SET NULL`, per DESIGN §4.4).
  **Rejected:** *a nullable `routine_id uuid` now, FK later* — two migrations
  and an unconstrained column in the interim, where a plain omission costs
  one. *Creating `routine` early to hang the FK on* — pulls a Tier-B table
  forward for a column no v1 M1 code reads. *Including `superset_group` as a
  bare nullable `smallint` now* — its semantics (grouping, ordering within a
  group, interaction with `position`) are Spec 09's to define, and a column
  shipped before its rules would be guessed at. **Consequence:** DESIGN §4.4's
  column lists are reconciled with a note (§11), so a reader does not conclude
  the design was missed.
- ✅ **D37 — `workout_exercise.exercise_id` is `ON DELETE CASCADE`, which
  discharges the purge-ordering obligation.** Specs 03.1 §3 and 03.2 §3 hand
  Spec 05 the question of how an account purge orders `workout_exercise`
  against the user's custom `exercise` rows (`exercise.owner_user_id` is itself
  `ON DELETE CASCADE`). With `CASCADE` on both paths (`user → workout →
  workout_exercise` and `user → exercise → workout_exercise`) no ordering is
  needed, and the cascade can only fire for the purged user's own rows because
  global exercises are never hard-deleted (DESIGN §4.2). AC17 proves it down
  both paths. **Rejected:** *`RESTRICT` / `NO ACTION`* — makes the purge depend
  on the order Postgres happens to process two cascades, and a purge that
  reaches an exercise row while `workout_exercise` rows still point at it is
  refused; it would push an explicit "delete these first" step onto Spec 11's
  purge job, where it is easy to omit and only fails in production against real
  data. *`SET NULL`* — `exercise_id` is `NOT NULL`, and a history row that has
  lost its exercise reference is worse than one that is gone with its owner;
  the snapshots (`NOT NULL`, written at add time) keep a session readable while
  its exercise lives, so the reference is not needed once the owner is purged.
  **Also settled here:** the two migration-shape choices that follow from this
  table's needs — the SQL is hand-authored (the partial unique index, the two
  `CHECK` lists and the deferrable constraint are outside the Prisma DSL, the
  `0002` carve-out), and there is no `change_xid`-style trigger (workouts have
  no delta-sync contract in v1, so 03.3's trigger would add write-path cost
  for no reader).
- ✅ **D38 — at most one in-progress session per user, enforced by a partial
  unique index; the `409` carries no workout id.** `workout_user_active_key`
  (`UNIQUE (user_id) WHERE ended_at IS NULL`) makes the rule a database
  invariant, so two concurrent creates cannot both pass a "do you have one?"
  read and both insert. A second create under a different `clientGeneratedId`
  is `409 workout-in-progress-exists`, surfaced by the `23505`-then-re-read
  path of D50. `GET /v1/workouts/active` returns the one
  matching row or `404`, and is how a client recovers from that `409` and how
  Spec 06 resumes a session after a cold load (Q12, Spec 04.0 §12). The problem
  body has no extension member for the id because `ProblemBody`
  (`apps/api/src/errors/problem.ts`) has none, and the recovery call costs one
  round trip on a path the user is already stalled on. **Rejected:** *a
  check-then-insert in the handler* — racy under concurrency, which is the case
  that matters here. *A per-user advisory lock around create* — a second
  mechanism doing the index's job, plus a serialization point on the most-called
  write (D40). *Allowing several in-progress workouts and letting the client
  pick* — pushes a disambiguation rule onto every client and makes "the active
  workout" ill-defined for resume. *Silently auto-finishing or discarding the
  old session on a new create* — destroys or mutates the user's data as a side
  effect of a retry-prone call. *Extending the Spec 01 problem+json contract
  with an id-carrying extension member* — widens a shared contract for a single
  error, permanently, to save one `GET`. **Consequence:** DESIGN §4.4 gains the
  invariant (§11).
- ✅ **D39 — an idempotent replay ignores the request body.** A replay with the
  same `(user, clientGeneratedId)` returns `200` with the **stored** row and no
  `Location` header, whatever a **valid** replayed body says. The body is
  still validated first: `CreateWorkoutSchema` and the §6.4 skew bounds run
  before the insert, so a replay that arrives outside the skew window is `422`
  by design rather than a `200` replay (§6.1) — validation is a property of
  the request, idempotency a property of the row. The case idempotency
  exists for is a retry over a flaky connection whose first attempt actually
  succeeded; answering `409` on a mismatch would turn that into an error a
  client cannot tell from a real conflict. The `200` / `201` split is the whole
  signal, and a client that cares whether it created the row reads the status.
  **Rejected:** *`409` on any body mismatch* — as above; also requires a
  field-by-field comparison whose definition of "same" (normalised
  timestamps, an omitted `tzOffsetMinutes` versus one resolved from
  `user.timezone`) is a spec of its own. *Compare and `409` only when the body
  differs* — the same cost with a rarer, harder-to-test failure. *Overwrite the
  stored row with the replayed body* (PUT semantics) — a retry could clobber a
  later edit, or rewrite `startedAt` and thereby `local_date`, breaking the
  "derived once, never re-derived" rule (§6.3). **Accepted cost:** a buggy or
  edited-then-retried client is not told its body was ignored; DESIGN §6
  ("replaying the same id returns the existing resource") is what it was
  promised.
- ✅ **D40 — the create path is one `ON CONFLICT DO NOTHING` insert with three
  statement outcomes, and takes no advisory lock.** Per §6.2: a returned row is
  a fresh create (`201`); zero rows means the idempotency key already existed,
  so re-read by that key and return it (`200`), while a re-read that finds
  nothing (the stored row was concurrently deleted) retries the insert
  **once**, and a second empty result fails as an internal error (`500`), so
  the loop is bounded; and SQLSTATE `23505` on the constraint named
  `workout_user_active_key`, mapped **after** the failed statement's rollback,
  is resolved by a re-read on the idempotency key — a hit is a replay (`200`),
  a miss is `409 workout-in-progress-exists` (D50). Any `23505` on another
  constraint is a bug and surfaces as `500`. The create is a single statement
  outside any interactive transaction, so that rollback costs nothing and the
  re-read runs on a clean connection.
  The exactly-one-winner guarantee comes from the two unique indexes (§4) and
  those re-reads, so **no per-user create lock** is needed. **Rejected:** *a
  per-user advisory lock around the create* — first, it is redundant: the
  arbiter index and the partial unique index already admit exactly one winner,
  and D50's re-read resolves the loser without coordination; second, it would
  serialize the app's most-called write, where D42's lock serializes only
  writes to the one workout that genuinely contend; and third, it would hold a
  pooled connection while blocked, the connection-pool exposure 03.2's D20
  left open for 05.2 (§12 Open). *A handler-level try/catch that answers `409`
  from the `23505` directly, without the re-read* — see D50; it makes the
  answer depend on which unique index the loser happened to reach first.
  *Matching on the Prisma error code alone* — a raw `$queryRaw` insert
  surfaces as `P2010` with the driver's SQLSTATE and message in `meta` (the
  constraint name is parsed from the message), not the typed client's `P2002`
  + `meta.target`; no repository parses these today, so AC5's unit test pins
  the idiom this spec introduces.
- ✅ **D41 — `workout_exercise.position` is unique per workout via a deferrable
  constraint, kept dense by the repository.** `workout_exercise_position_key`
  is `UNIQUE (workout_id, position) DEFERRABLE INITIALLY IMMEDIATE`, and add,
  reorder and delete run `SET CONSTRAINTS workout_exercise_position_key
  DEFERRED` in their transaction, so the intermediate states a swap or a
  set-based shift passes through (two rows sharing a position) are checked only
  at `COMMIT`. `INITIALLY IMMEDIATE` keeps every other write strict. It must be
  a **constraint**, because Postgres cannot defer a bare unique index; and a
  deferrable constraint cannot be an `ON CONFLICT` arbiter, which is harmless
  since nothing here upserts on position (the one `ON CONFLICT` in this spec
  uses the non-deferrable `workout_user_client_id_key`). **Rejected:** *no
  unique constraint* — a duplicate position is then silent, shows up as a list
  that reorders itself between reads, and cannot be recovered from the data;
  the constraint costs one `SET CONSTRAINTS` on paths that already do
  renumbering. *Fractional / float positions* — right for collaborative
  documents with thousands of items and no write coordination; here a workout
  has a few dozen exercises and one writer, a renumber is a single-digit-row
  `UPDATE`, and fractions bring unbounded precision drift, a `numeric` column
  and a compaction job nobody would remember to write. *A non-deferrable
  constraint with an ordered, one-row-at-a-time shift* — fragile against the
  plan's row order and fails a swap outright, since no ordering of a two-row
  swap avoids a transient duplicate. **Consequence:** DESIGN §4.4 states the
  dense-sequence invariant (§11).
- ✅ **D42 — position mutations take a per-workout advisory lock, and it is
  the only advisory lock in this spec.** Add, reorder and delete each read the
  current count and then write positions that depend on it; two concurrent adds
  could both compute the same `n` and collide at `COMMIT`, when the deferred
  check runs and it is too late to renumber. The lock is
  `pg_advisory_xact_lock(hashtext(workoutId))`, transaction-scoped, issued as
  its **own statement** ahead of the count read, following `insertWithCap` and
  03.2's D23: under `READ COMMITTED` a statement's snapshot is fixed at its
  start, so a lock folded into a CTE alongside the read would still read a
  stale count. A `hashtext` collision between two workouts costs a
  short serialization of unrelated callers, never correctness, and the key is
  not attacker-chosen because a caller cannot address a workout that is not
  theirs. **This lock does not close D20.** It is a correctness lock scoped to
  one workout and held for one short transaction; it is not the per-user
  create lock D40 rejects, and this spec makes no claim that it bounds a
  single account's pool usage (§7). **Rejected:** *`SELECT … FOR UPDATE` on the
  parent `workout` row* — serializes against every other write to the workout
  (a `PATCH` of `title`, a finish) that has no reason to wait on a reorder, and
  is not a lock this codebase already has an idiom for. *Relying on the
  deferred unique check to make the loser retry* — surfaces as a `23505` `500`
  at `COMMIT` and would need a retry loop in the repository. *Optimistic
  version columns* — a second concurrency mechanism for a case where the
  posture is last-write-wins (§6.8).
- ✅ **D43 — position range checks live in the repository layer, not the
  schema.** The DTOs bound `position` below only (`.min(0)`); the repository
  enforces add `0 ≤ position ≤ n`, reorder `0 ≤ position ≤ n-1`, and answers
  `422 validation-error` otherwise. The schema cannot know `n`, and this is
  also the only thing that keeps `position` inside `smallint`: without it a
  client sending `2147483647` reaches Postgres as a `22003` numeric-overflow
  `500`. Since `n` for a real workout is a few dozen, the check rejects
  everything above 32767 long before the column sees it, so an implementer
  must treat it as load-bearing. **Rejected:** *a static schema upper bound
  (`.max(32767)`)* — tracks the column's type rather than the actual
  sequence, so `position = 900` on a three-exercise workout would pass
  validation and break density. *Clamping an out-of-range position to the end*
  — silently does something other than what the client asked and hides
  client bugs. *Letting Postgres reject it and mapping `22003`* — a `500` path
  for a client error, and no defense for values that are in `smallint` range
  but outside `0 … n`.
- ✅ **D44 — finish is a `PATCH`, and `workout-finished` is the single
  immutability signal, with `DELETE /v1/workouts/{id}` its one exemption.**
  Setting `endedAt` through `PATCH /v1/workouts/{id}` is the finish transition;
  there is no `POST .../finish`. A finished workout rejects every `PATCH`
  (including `PATCH {}` and `PATCH { endedAt: null }`), and the add, `PATCH`
  and `DELETE` of a `workout_exercise` under it, with `409 workout-finished`;
  the check runs on the target's state **before** the body is inspected. On an
  in-progress workout, `PATCH {}` and `endedAt: null` are `200` no-ops (the
  schema accepts `null` so un-finish reaches the handler and is answered
  rather than `422`d). Whole-workout `DELETE` is allowed in both states — the
  sanctioned escape hatch from DESIGN §4.5's immutability — and a repeat is
  `404`. **Why:** a client handles one slug for "the parent is immutable"
  across four writes, and immutability is what lets Spec 07's PR cache avoid
  an invalidation path. **Rejected:** *`POST .../finish`* — one column, one
  more route and schema, and it could not finish atomically with a last note.
  *`PATCH {}` uniquely succeeding on a finished workout* — makes the client
  model two rules ("finished rejects writes, except the empty one") to save a
  refetch `GET` already provides. *`422` for `endedAt: null`* — the body is
  valid; the target's state is what conflicts (03.2 D19's `409` rule).
  *Distinct slugs per write* — four things to switch on for one condition. *Also
  blocking `DELETE /v1/workouts/{id}`* — leaves a user no way to remove a
  session, which DESIGN §4.9 promises. **Consequence:** the house style is one
  `AppError` class per business rule, so `WorkoutFinishedError` and
  `WorkoutInProgressExistsError` are separate classes with no generic
  `ConflictError`.
- ✅ **D45 — a malformed `{id}` path parameter is `404 not-found`, not
  `422`.** Path ids are declared `z.string()` and the repository turns a
  malformed value into `NotFoundError` with the branded guard before any query,
  as `exercise.prisma.ts` does with `isExerciseId`. This follows 03.1 / 03.2,
  whose blocks list no `422` for a bad id, and it means "wrong shape" and "not
  yours" are indistinguishable, so a caller cannot use response codes to probe
  id validity. A malformed `exerciseId` in a request **body** stays `422`: that
  is body validation. **Rejected:** *`422` for a malformed path id* — the more
  conventional REST answer, but it departs from the shipped catalog routes and
  gives a prober one bit of information (this shape is valid) for nothing.
  *A branded-UUID Zod schema on the path* — moves the check into the route
  layer, so it fires before ownership and yields `422` unless separately
  overridden.
- ✅ **D46 — free-text bounds are `title` 1–120 and `notes` 1–4000 UTF-16 code
  units, and `null` is the single absent form.** `WORKOUT_TITLE_MAX` (120, the
  `CatalogName` ceiling; a single line in a list row) and `WORKOUT_NOTES_MAX`
  (4000, several paragraphs, too small to serve as free storage) are code
  constants in `@sin/core`. Zod counts UTF-16 code units, so an emoji costs
  two, which is stricter than `text` needs and the safe direction for a bound.
  The minimum of 1 means `""` is `422`; a client that wants "no title" sends
  `null` or omits the key, so the column has one absent representation
  (`NULL`). Both fields reject control characters at ingress (`title`: every C0
  plus DEL; `notes`: the same except TAB, LF and CR) and are stored verbatim,
  with output encoding left to Spec 06's renderer (§7). The worst case is
  well inside the 64 KiB body limit, so an over-long field is always a `422`
  naming the field, never a `413` with no field path. **Rejected:** *counting
  bytes or grapheme clusters* — more machinery and no realistic gain for
  a bound whose job is to stop abuse. *Allowing `""` alongside `null`* — two
  absent representations every reader must collapse. *A single shared length
  for both* — a title is one line and notes are prose; one bound fits neither.
  *Sanitising or HTML-escaping on write* — wrong for every non-HTML consumer
  (JSON API, native clients, export) and double-encodes under React.
- ✅ **D47 — `startedAt` is bounded at +5 minutes future and −7 days past
  against the server clock; `endedAt` on finish uses the future bound.** The
  bounds are handler rules raising `ValidationError` (`422`), not Zod
  refinements, because a schema sees the body but not the clock. They are code
  constants in `packages/core/src/dto/workout.ts`, alongside
  `WORKOUT_TITLE_MAX`, in **milliseconds** (the unit of the `Date.now()`
  arithmetic they feed, and the one the `BODY_LIMIT_BYTES` naming precedent
  carries in the identifier):
  ```ts
  export const WORKOUT_FUTURE_SKEW_MAX_MS = 5 * 60 * 1000;          // 300_000
  export const WORKOUT_STARTED_AT_PAST_MAX_MS = 7 * 24 * 60 * 60 * 1000; // 604_800_000
  ```
  `WORKOUT_FUTURE_SKEW_MAX_MS` is deliberately not `STARTED_AT`-prefixed
  because it also bounds `endedAt`; the past bound applies to `startedAt` alone
  (`endedAt` is already floored by `endedAt ≥ startedAt`). A timestamp is
  rejected when it is **strictly** beyond a bound — exactly at the limit is
  accepted — matching "more than" in §6.4, and a pure helper takes an injected
  `now` so the boundary is unit-testable. **Why these values, and why
  asymmetric.** The only v1 writer is "start a session now" (Spec 06), so a
  legitimate `startedAt` is essentially the present; 5 minutes covers ordinary
  phone clock drift with headroom, and a future `startedAt` is never
  legitimate — it would also sort to the top of Spec 07's `(user_id,
  started_at DESC)` history and hold the in-progress slot. The past side is
  looser because lag in that direction is legitimate: a client that queued a
  create while offline (Risk R1's retry queue) and drains it later, or a user
  reopening the app after a week with a mis-set clock, should not be blocked
  outright; 7 days still bounds a device that reset its clock to a stale date.
  Both are guards against a device clock poisoning history ordering, not
  product rules. **Rejected:** *no bound* — one bad clock permanently corrupts
  the history sort for that user. *Symmetric tight bounds (say ±5 minutes)* —
  rejects the legitimate queued or delayed create. *A tighter past bound (24
  hours)* — blocks the recoverable offline case for no ordering benefit.
  *Env-configurable bounds* — a staging value could silently differ from
  production, and the behavior could no longer be stated as a number (§8).
  **Consequence:** widening either bound later is a backward-compatible
  relaxation; narrowing is not, which is the argument for erring generous now.
  DESIGN §6's "Time" bullet gains the rule (§11).
- ✅ **D48 — `@sin/core` DTO naming: `XxxSchema` for the Zod value, bare `Xxx`
  for the inferred type, no `Body` / `Response` suffix.** The core package
  carries more than one style today, so this is a majority choice, not
  unanimity: 03.1 and 03.2 use `ExerciseSchema` / `Exercise` and
  `CreateExerciseSchema` / `CreateExercise`; Spec 02 uses `MeSchema` / `Me` but
  names its update type `UpdateMeInput`; 03.3 introduced suffix-less values
  (`ExercisesResponse`, `MuscleGroupsResponse`) with `Body`-suffixed types
  (`ExercisesResponseBody`). `Schema` wins because it is the existing
  majority and what the branded ids already use (`UserIdSchema`,
  `ExerciseIdSchema`); because the runtime value and the compile-time type
  read as distinct at a glance (`WorkoutSchema.parse(x)` versus `Workout`);
  and because it needs no per-type suffix decision — the type is the bare
  noun. **Rejected:** *03.3's suffix-less values with `Body`-suffixed types* —
  a minority, and the value and type names then differ by a suffix that
  encodes no distinction a reader needs. *`Input` / `Output` type suffixes
  (as `UpdateMeInput`)* — sound where a schema's input and output types
  differ, but nothing in this spec's DTOs transforms, so the suffix would be
  noise. **Not renamed:** Spec 02's `UpdateMeInput` and 03.3's shipped exports
  stay as they are; renaming is churn on a merged wire contract for no
  behavioral gain, and is its own change if ever wanted.
- ✅ **D49 — AC1's "no drift between the migrated database and
  `schema.prisma`" assertion lives in this spec's migration integration test,
  not in a new CI job.** It follows 03.3 D30's pattern: the migration
  integration test runs `prisma migrate diff --from-url … --to-schema-datamodel
  … --exit-code` through `spawnSync` (so a CLI that fails to run at all fails
  the test rather than looking like "no diff") and expects "No difference
  detected". The hand-authored SQL (D37) is exactly the situation that drifts:
  CI's `prisma validate` sees the schema but not the migrated database, and
  drift surfaces later as a phantom `migrate dev` migration. **Rejected:** *a
  dedicated CI step* — a second place to keep in step with the integration
  job's database setup, when the test already holds a migrated container.
  *Relying on `prisma validate` alone* — cannot see model/migration drift
  (03.3 D30). **Consequence:** no new CI step; the existing `prisma validate`
  remains the only pipeline item for schema shape.
- ✅ **D50 — a `23505` on `workout_user_active_key` is resolved by re-reading
  the idempotency key before it becomes a `409`.** The create path's error
  branch does not answer `409 workout-in-progress-exists` from the SQLSTATE
  alone: after the failed statement's rollback it re-reads by
  `(user_id, client_generated_id)`; a hit means this request was a same-key
  replay that raced its own original, so it answers `200` with the stored row,
  and only a miss is the real "you already have a workout in progress" `409`
  (§6.2, AC5). **Why.** The tempting shortcut is to argue that a same-key
  retry can never reach this branch, because `ON CONFLICT`'s pre-check
  consults the **arbiter** index first and a conflict there abandons the tuple
  before any other index is touched. That reasoning holds only when the
  conflicting row is already committed, or is in flight and visible to the
  pre-check's dirty snapshot (which waits on it and re-checks). It fails when
  a rival transaction has not yet inserted its own arbiter index tuple: both
  requests pass the pre-check and speculate, and the loser resolves its
  conflict on whichever unique index it reaches first — index tuples are
  inserted in index-OID order, i.e. in the order the two `CREATE` statements
  happen to sit in the hand-authored `0005` migration. Correctness must not
  depend on that. The re-read decides the outcome from data instead, for one
  `SELECT` on an error path that is already rare. It also makes AC3's ten-way
  concurrent-replay assertion deterministic rather than schedule-dependent.
  **Rejected:** *pinning the migration's statement order so
  `workout_user_client_id_key` always has the lower OID, with an AC1
  assertion on the two indexes' relative OIDs* — it does work, but it makes a
  wire-visible status code depend on a detail of migration authoring that no
  reviewer would think to protect, and the AC1 assertion protects it only
  against a rewrite of `0005`, not against a future index rebuild or a
  `pg_dump`/restore that reassigns OIDs. Harmless as belt-and-braces, and an
  implementer may keep the two `CREATE`s in that order for tidiness, but it is
  not the mechanism this spec relies on. *Answering `409` and letting the
  client sort it out via `GET /v1/workouts/active`* — the client would see a
  `409` for a request that in fact succeeded, which is exactly the
  indistinguishable-from-a-real-conflict failure D39 exists to avoid.
  **Consequence:** D40's error branch and AC5's unit cases carry the re-read;
  the create path stays lock-free.

### Open

Explicitly **not resolved here**; each is owned by Spec 05.2, where the rate
limiter and per-user write quotas are introduced (the repository has no
rate-limit plugin today, so 05.2 is greenfield — §7).

- **03.2's D20 — the advisory-lock connection-pool blast radius.** A burst of
  concurrent writes from one account can occupy several of the app's 8 pooled
  connections and back up other users' requests. 05.0 adds no such exposure on
  the create path (D40 takes no lock) but **does add a per-workout advisory
  lock on position mutations (D42), and that lock does not close D20**: it is
  scoped to one workout and one short transaction, and says nothing about how
  many pooled connections one account may hold at once. Owner: **05.2**.
- **03.2's D21 — unbounded growth via create/delete cycling.** The
  one-in-progress rule (D38) bounds *concurrent* open sessions to one per user
  and limits nothing about *sequential* volume, so an authenticated account can
  still create, delete and re-create workouts, and add exercises, as fast as
  the connection allows. The only volume bounds in this spec are per-request:
  bounded free text and the 64 KiB body limit. Owner: **05.2**.
- **The `docs/specs/README.md` question, "pull a minimal per-user create quota
  forward, or record the gap?"** (raised for 03.2, which recorded the gap as
  D21). Still open for 05.0's own create path; 05.0 takes the same position as
  03.2 and ships no quota. Owner: **05.2**, which decides it together with D20
  and D21 rather than piecemeal.
